# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Trakka is an ultra-lightweight Go backend (target: <20MB RAM at runtime) for shopping lists and to-do lists, exposed as a JSON REST API and served alongside static PWA assets. It is designed to run identically under Docker and Podman rootless, and is held to explicit security rules (parameterized SQL only, strict URL scheme validation, hardened HTTP response headers) — see the "Security rules" section below before touching handlers or the frontend.

## Commands

```bash
go build -o trakka ./cmd/server    # compile
go vet ./...                       # static checks
gofmt -l .                         # list any non-gofmt-formatted files (should be empty)
go mod tidy                        # sync go.mod/go.sum after dependency changes

# Run locally (no container needed — pure Go, no CGO)
PORT=8080 DB_PATH=./trakka.db STATIC_DIR=./static TEMPLATES_DIR=./templates go run ./cmd/server
```

There is no test suite yet (`go test ./...` has nothing to run). If you add tests, standard `go test ./...` applies; there's no custom test runner or build tag scheme.

### Container workflow

```bash
docker compose up -d --build              # Trakka only
docker compose --profile calendar up -d   # Trakka + Radicale (CalDAV sync)
podman-compose up -d                      # same compose.yml works unchanged
```

## Architecture

The backend is split into a thin entry point plus several internal packages, deliberately separated so persistence, HTTP concerns, auth logic, and shared types don't bleed into each other:

```
cmd/server/main.go     entry point: config, wiring, graceful shutdown, -healthcheck probe
internal/config/       env-var config loading
internal/models/       House/List/Item/User/Session/HouseMember structs shared by db and handlers
internal/db/           all database/sql usage lives here — nowhere else imports it
internal/auth/         non-HTTP auth logic: bcrypt, session tokens, hand-rolled OIDC client
internal/handlers/     routing, request validation, JSON responses, session middleware, /auth/... handlers
internal/validate/     small reusable input validators (currently: URL scheme)
internal/scraper/      best-effort background price lookup for an item's URL (OpenGraph/JSON-LD/microdata)
static/                PWA assets served at "/" (index.html, js/, locales/ (FR/EN dictionaries), sw.js, manifest.json, icons/)
templates/             login.html, rendered via html/template
```

`templates/login.html` (via `html/template`, `Content-Type: text/html`) is the one exception to "no server-side rendering" — it genuinely needs server-injected per-request data (whether OIDC is configured, login-vs-register mode, a canned error message), which nothing else in this app does. `internal/handlers.Routes()` still serves `static/` purely via `http.FileServer`; don't add further `html/template` rendering unless something else genuinely needs server-injected per-request data the same way.

- **`internal/handlers` never imports `database/sql`** — it only calls methods on `*db.DB` (e.g. `GetList`, `CreateItem`) and checks for the sentinel `db.ErrNotFound` to decide between a 404 and a 500. Keep that boundary: if a handler needs a new query, add a method to `internal/db`, don't reach around it.
- **Routing** uses the Go 1.22+ standard-library `http.ServeMux` method-pattern syntax (`mux.HandleFunc("GET /api/v1/lists", ...)`) in `internal/handlers/app.go` — there is no router dependency (no Gin, no chi). Unmatched methods on a registered path pattern get an automatic 405 from the stdlib mux; don't hand-roll that check in handlers.
- **Database**: `modernc.org/sqlite` (pure Go, no CGO) — this is load-bearing for the static-binary/no-CGO build in the Dockerfile. Do not swap in `mattn/go-sqlite3` or any CGO-based driver.
  - The DB connection pool is deliberately capped at `SetMaxOpenConns(1)` / `SetMaxIdleConns(1)` (see `Open()` in `internal/db/db.go`). SQLite only allows one writer at a time and modernc's driver gains nothing from pooling; this avoids `SQLITE_BUSY` errors under concurrent requests. Don't "fix" this by raising the pool size without understanding why it's there.
  - Schema lives in a versioned sequence of migration files, `internal/db/migrations/NNNN_description.sql` (which is why that directory must live inside the `internal/db` package, not at the repo root), embedded into the binary via `//go:embed migrations/*.sql` and applied by the engine in `internal/db/migrate.go`, tracked through SQLite's built-in `PRAGMA user_version` rather than a separate version table or a third-party tool like `go-migrate` (kept in-house per the "standard library wherever possible" convention below). Each migration runs inside its own transaction that also bumps `user_version` on success, so a crash mid-migration can never leave the schema and its recorded version disagreeing with each other; each is applied at most once per database, ever, so — unlike the old idempotent-`schema.sql` approach this replaced — a migration file needs no `IF NOT EXISTS`/existence guards of its own. A database that predates this system (tables already exist, but `user_version` is still `0`, SQLite's untouched default) is detected (`hasExistingSchema`) and adopted directly at the latest version without re-running any migration SQL, rather than replaying scripts that would fail outright on already-existing tables/columns. Before applying a pending migration to an already-versioned, non-empty database, `backupBeforeMigration` snapshots it via `VACUUM INTO` (a single ordinary SQL statement, no CGO backup API needed) to `<DB_PATH's directory>/backups/` — `/data/backups/` for the default `DB_PATH`. To evolve the schema, add a new `internal/db/migrations/NNNN_description.sql` file rather than editing an already-shipped one in place (existing deployed `/data/trakka.db` files have already recorded that migration as applied and will never run it again). One migration (widening the `lists.type` `CHECK` constraint, which needs `PRAGMA foreign_keys = OFF` set *outside* any transaction — something the engine's generic one-script-one-transaction wrapper can't express) is registered as Go code instead of a `.sql` file; see `internal/db/migrate_list_types.go`. See [docs/DATABASE.md](docs/DATABASE.md#evolving-the-schema) for the full design.
- **Go version pinning is intentional and fragile**: `go.mod` currently declares `go 1.27.0` and pins `modernc.org/sqlite v1.57.0` (its own `go.mod` requires `go 1.25.0`, comfortably under 1.27.0), matching the `golang:1.27.0-alpine` build stage in the `Dockerfile`. These three numbers (go.mod's `go` directive, the sqlite driver version, and the Dockerfile's builder image tag) must move together. Running `go mod tidy` carelessly (e.g. `go get -u`) will pull a newer `modernc.org/sqlite` that bumps the required Go version past what the Dockerfile's builder image provides, silently breaking the Docker build stage without touching the Dockerfile. If you upgrade the driver, bump the Dockerfile's builder image in the same change (pin an exact `golang:<version>-alpine` tag — Docker Hub publishes one per patch release — rather than a floating minor-only tag, so the build doesn't depend on when that tag last moved) and verify `go.mod`'s `go` directive still matches.
- **Config is env-var only**, loaded once in `internal/config.Load()`: `PORT` (default `8080`), `DB_PATH` (default `/data/trakka.db`), `STATIC_DIR` (default `/app/static`), `TEMPLATES_DIR` (default `/app/templates`), `BASE_URL` (required only if OIDC is configured), `OIDC_ISSUER`/`OIDC_CLIENT_ID`/`OIDC_CLIENT_SECRET` (all-or-nothing — `Config.Validate()`, called from `main.go` right after `Load()`, errors out at startup if only some are set), `SESSION_COOKIE_SECURE` (default `true`), `SESSION_TTL_HOURS` (default `720`). No config file, no flags other than `-healthcheck`.
- **Healthcheck is in-process**: `trakka -healthcheck` (implemented in `cmd/server/main.go`) does an HTTP GET against its own `/healthz` and exits 0/1. This is what `Dockerfile`'s `HEALTHCHECK` and `compose.yml`'s `healthcheck.test` both call — it exists specifically so the runtime image doesn't need `curl`/`wget` installed. If you change the healthcheck behavior, update both call sites.
- **Graceful shutdown**: `cmd/server/main.go` selects on a signal channel and a server-error channel; on SIGINT/SIGTERM it calls `srv.Shutdown(ctx)` and only then returns from `main`, which runs the deferred `database.Close()`. Don't call `os.Exit()` on the shutdown path — it would skip that deferred close.
- **Logging** uses `log/slog` (structured JSON to stdout), not the plain `log` package. `internal/handlers.Recover` is a last-resort panic safety net, not a substitute for the explicit `if err != nil` handling used everywhere else — every predictable failure path already returns an error before it would reach that middleware.
- **API shape**: `/api/v1/houses` (groups lists; a list belongs to exactly one house via `house_id`; a house has one or more members via `house_members`), `/api/v1/houses/{id}/members` (roster + invite-by-email + remove), `/api/v1/lists` (filterable by `?house_id=`/`?type=`/`?shared_with_me=true`; a list `GET` by id embeds its `items`), `/api/v1/items` (filterable by `?list_id=`, with `PATCH` supporting partial updates like toggling `done` without resending the whole item), `/api/v1/custom-categories` (`GET`/`POST`/`PUT /{id}`/`DELETE /{id}`, no partial `PATCH`), and `/api/v1/custom-categories/{id}/share`/`/api/v1/lists/{id}/share` (granular sharing — see the dedicated bullet below). Full endpoint reference: [docs/API.md](docs/API.md).
- **Custom categories ("Spaces")**: a `CustomCategory` (`id`, `user_id`, `name`, `icon`, `color`, `position`, `created_at`) is a personal, freeform way to organize lists beyond the fixed `lists.type` enum — e.g. "Vacances", "Anniversaire de Léo" — orthogonal to `type` rather than a replacement for it. Unlike every other grouping concept in this app (`houses`, shared by all `house_members`), a category is owned by exactly one user (`user_id`) and is never itself shared: `internal/db/custom_categories.go`'s `GetCustomCategoryForUser`/`UpdateCustomCategoryForUser`/`DeleteCustomCategoryForUser` all scope their `WHERE` clause on `user_id`, so a category can only ever be found, edited, or deleted by whoever created it — a mismatched owner comes back as the same `ErrNotFound` as a nonexistent id (never a separate 403), the same "don't let a lookup confirm another user's data exists" reasoning `UpdateCustomCategoryForUser` shares with `authorizeHouseAccess`'s sibling checks. `lists.custom_category_id` (`INTEGER`, nullable, `addColumnIfMissing`-guarded like `house_id`, `REFERENCES custom_categories(id) ON DELETE SET NULL`) is how a list attaches to one — `ON DELETE SET NULL` rather than `CASCADE` is deliberate, since deleting a category should only unassign it from any list that used it, not delete the list itself. Any member of the list's house can associate or dissociate a category via `POST`/`PUT /api/v1/lists` (`custom_category_id` in the request body; omitted/`null` on the `PUT` full-replace dissociates, matching how `type` already resets to its default when omitted there), but the id must reference a category the *requesting* user owns (`internal/handlers.validateCustomCategoryOwnership`, `internal/handlers/lists.go`) — a shared list can be tagged with one member's personal category, but not with a category belonging to someone else in the house. `GET /api/v1/lists`/`GET /api/v1/lists/{id}` embed the attached category's full row (`custom_category`) via a `LEFT JOIN` in `internal/db.listSelect`, visible to every house member regardless of who owns the category, since seeing a shared list's category tag doesn't require owning it. The frontend (`static/js/spaces.js`) is a fourth dashboard tab, "Espaces" (`#tab-spaces`/`#spaces-view`), rendering each category as a collapsible `<details>` section (mirroring `#done-section`'s chevron/`group-open:` idiom) holding every list attached to it regardless of `type` — see the "Custom categories frontend" bullet below for the full design.
- **Granular sharing (Spaces & Lists)**: beyond house membership, a Space (a `custom_categories` row) or an individual List can be shared directly with one other user by email, giving them `read` or `write` access to it without adding them to the whole parent house — two new tables, `space_shares` and `list_shares` (`id`, `custom_category_id`/`list_id`, `shared_with_user_id`, `permission` ∈ `read`/`write`, `created_at`, each with a `UNIQUE (…, shared_with_user_id)` constraint so re-sharing with a different permission upserts via `internal/db.CreateOrUpdateSpaceShare`/`CreateOrUpdateListShare` rather than erroring). `internal/db.AccessLevelForList` is the single place that combines the three access sources into one verdict for a given user and list — house membership (always `write`, unchanged from before this feature), a `list_shares` grant on the list itself, and a `space_shares` grant on the list's `custom_category_id` if any, taking the higher of the two shares when both apply. `internal/handlers.authorizeListAccess`/`authorizeItemAccess` (`internal/handlers/rbac.go`) replace the old house-only checks on every list/item read and mutation handler, each now taking a `requireWrite bool` — read-only handlers (`GET`) accept `read`, mutating ones (`POST`/`PUT`/`PATCH`/`DELETE`) require `write` — with one deliberate exception: `DELETE /api/v1/lists/{id}` stays House-membership-only even for a `write` share holder, since destroying a whole list (and everyone else's items in it) is a house-management-level action, not just editing. Sharing a **Space** is the owning user's call alone (`internal/handlers.authorizeSpaceOwner`, the same check as renaming/deleting the category); sharing a **List** requires actual House membership of it (`authorizeHouseAccess`, not merely `write` access via another share), so access granted through a share can never itself be used to extend further access. `internal/handlers/shares.go` exposes `GET`/`POST /api/v1/{custom-categories,lists}/{id}/share` and `DELETE .../{id}/share/{userId}` (mirroring the roster/invite/remove shape of `/houses/{id}/members`); `GET /api/v1/lists?shared_with_me=true` (`internal/db.ListSharedListsForUser`) is the reverse query backing the "Partagé avec moi" dashboard tab — every list reachable via either share table, excluding any whose house the caller is already a plain member of (already visible the ordinary way). The frontend (`static/js/shares.js`) owns a shared create/revoke modal (`#share-modal`, opened via `openShareModal({kind, id, name})` from a 👥 button on every list card/space section and the list-detail header) and the fifth dashboard tab (`#tab-shared`/`#shared-view`, wired into `planning.js`'s `setActiveTab` switcher alongside the other four); a card reached via `?shared_with_me=true` shows a 👥 "Partagé" badge (from `list.access_source`, a response-only field like `Item.PriceStatus`) and no edit/share/delete controls, since managing a list requires actual House membership. Full design and endpoint reference: [docs/API.md](docs/API.md#sharing) and [docs/DATABASE.md](docs/DATABASE.md#space_shares-and-list_shares).
- **Pinning shared lists (and shared Spaces) to the dashboard**: the recipient of a share can choose to have it show up alongside their own house's lists on the main dashboard, instead of having to go find it in the "Partagé avec moi"/"Espaces" tabs every time. Two independent pin flags, combined via OR: `list_shares.is_pinned_to_dashboard` (`INTEGER NOT NULL DEFAULT 0`, migration 11) pins one List, toggled via `PATCH /api/v1/lists/{id}/share/pin` (`internal/handlers.handleListSharePin`/`internal/db.SetListSharePinned`); `space_shares.is_pinned_to_dashboard` (migration 12) pins a whole Space — every List reachable through it, present and future, no per-list action needed — toggled via `PATCH /api/v1/custom-categories/{id}/share/pin` (`internal/handlers.handleSpaceSharePin`/`internal/db.SetSpaceSharePinned`). Both endpoints' only authorization check is "does the caller hold a share row for this List/Space at all" (surfaced as `404`, not `403`, matching this file's existing "don't distinguish nonexistent from unauthorized" convention) — there's no house-membership or ownership check, since this is the recipient's own preference about their own dashboard, not a change to who can access anything. A List reached *only* through a shared Space (no `list_shares` row of its own yet) can still be pinned individually: `SetListSharePinned` auto-creates that row on demand, scoped to exactly the permission the Space already grants for that List (via a `lists JOIN space_shares` lookup on `custom_category_id`), so the insert can never itself change the recipient's `AccessLevelForList` verdict — it just gives the individual pin flag somewhere to live. `db.ListSharedListsForUser` (the same query backing `?shared_with_me=true`) surfaces the combined state as `List.IsPinnedToDashboard`, a response-only field alongside `AccessSource`/`AccessPermission`: for a `space_share`-sourced row this now comes from `space_shares.is_pinned_to_dashboard` directly (pinning the Space) rather than a hardcoded `false`; when a List is reachable both via its own `list_shares` row and via its Space, whichever source says pinned wins — the same "OR", not "AND", the higher-of-two-permissions dedup already uses — so unpinning one List individually cannot override its parent Space being pinned as a whole; the Space-level pin is the "master" pin for everything reachable through it. `db.ListSpacesSharedWithUser` (backing `GET /api/v1/custom-categories?shared_with_me=true`) is the Space-level "shared with me" query, the category-level equivalent of `ListSharedListsForUser`, surfacing `AccessPermission`/`IsPinnedToDashboard` response-only fields on `CustomCategory` the same way. The frontend has no server-side "house lists plus pinned shares in one call" endpoint to lean on: `static/js/app.js`'s `loadDashboard` fetches its usual `?house_id=` listing, then separately calls `static/js/shares.js`'s `loadPinnedSharedLists` (a thin wrapper around `?shared_with_me=true`, filtered to `is_pinned_to_dashboard`) and merges the two client-side before splitting into the three dashboard grids — a best-effort merge (`.catch(() => [])`) so a failure fetching pinned shares can never block the rest of the dashboard from rendering, and, like the rest of the sharing feature, with no offline mirror (pinned shared cards simply aren't shown while offline). `buildListCard` (`app.js`) grows a 📌/📌-filled pin/unpin button (`toggleListPin`, a plain await-then-`refreshVisibleView()` mutation, not the coalesced-optimistic pattern item quantity/urgent toggles use, since this is an infrequent deliberate action rather than a rapid-fire control) shown for both `list.access_source === 'list_share'` and `'space_share'` — the same 👥 "Partagé" badge the granular-sharing feature already renders for any `access_source` is reused as-is for the "distinguish a pinned card from an owned one" requirement, plus a small 📌 "Épinglée" badge shown whenever `is_pinned_to_dashboard` is true, independent of the button's own state. `static/js/spaces.js`'s "Espaces" tab gains an "Espaces partagés avec moi" section (`buildSharedCategorySection`) listing every Space shared with the caller (pinned or not, so there's somewhere to discover and toggle the pin in the first place) with a `[⋮]` kebab opening `#space-card-actions-sheet` for the pin/unpin toggle — a Space's own lists there are drawn from `?shared_with_me=true`, filtered to `custom_category_id`s actually present in that shared-categories list (so a List shared *individually* that happens to sit in the owner's other, unshared category is never incorrectly grouped under a "shared space" section). Full design and endpoint reference: [docs/API.md](docs/API.md#patch-apiv1listsidsharepin), [docs/API.md](docs/API.md#patch-apiv1custom-categoriesidsharepin) and [docs/DATABASE.md](docs/DATABASE.md#space_shares-and-list_shares).
- **Recurring items**: an item can carry `recurrence_rule` (`TEXT`, nullable — one of `DAILY`/`WEEKLY`/`MONTHLY`/`YEARLY` or the custom `EVERY_X_DAYS:<n>` form, validated by `internal/validate.Recurrence`), `due_date` (`TEXT`, `YYYY-MM-DD`, validated by `internal/validate.Date`), and `recurrence_end_date` (same format/validator). All three are `addColumnIfMissing`-guarded the same way as `price`/`target_month`. `items.is_recurring` (`INTEGER NOT NULL DEFAULT 0`) is never a separate input anywhere in the stack — `internal/db.CreateItem`/`UpdateItem` always write it as `recurrence_rule IS NOT NULL`, so the two columns can never disagree. The behavior that makes this "recurring" rather than just three inert columns lives in `internal/handlers.applyRecurrenceCompletion` (`internal/handlers/recurrence.go`), called from `handleItemsUpdate`/`handleItemsPatch` right before the final `db.UpdateItem`: when a recurring item's `done` transitions `false → true` (checked against the item's *previous* `Done`, the same pattern `previousURL` already uses for the image-invalidation logic), it computes the next occurrence's `due_date` via `nextDueDate` (advancing from the item's current `due_date`, or from today if it has none yet, by the rule) and flips `Done` back to `false` — the item is un-checked with an advanced date instead of staying marked done, and instead of cloning a new row per occurrence, so a recurring chore never accumulates one item per completion (the "décale simplement la date" alternative, chosen for exactly that reason). If `recurrence_end_date` is set and the computed next occurrence would land after it (a plain string comparison, since both are `YYYY-MM-DD` and stay lexicographically comparable), the item is left `done: true` instead and never advances again. Clearing `recurrence_rule` (empty string via `PATCH`, or omitting it via `PUT`) clears `due_date`/`recurrence_end_date` along with it, since neither means anything once an item stops recurring. The frontend's recurrence `<select>` (`static/js/list_view.js`, in both the create-item form and the edit-item modal, not gated by `data-shopping-only` since a recurring chore applies to `todo` lists just as naturally as a shopping item) only offers the four fixed cadences — `EVERY_X_DAYS:<n>` is API-only, accepted for forward compatibility but with no UI to set it yet. A recurring item's row shows a small 🔄 badge with the translated frequency (`recurrenceBadgeLabel`/`buildRecurrenceBadge`) that stays in place across completions. Because the PATCH that checks an item off can be queued offline, `static/sw.js` carries a hand-kept JS port of the same completion logic (`applyRecurrenceCompletionOffline`/`nextDueDateOffline`, called from `applyOptimisticEdit` and `resolveAgainstPendingCreate`) so a recurring item checked off while offline advances immediately in the local optimistic state instead of sitting "done" until the sync queue flushes and a refetch corrects it — **any change to the Go rule-handling must be mirrored there by hand**, since there's no code sharing between the two runtimes. Verified end-to-end in three real headless-Chromium (Playwright) sessions: create-with-recurrence via the item form and badge rendering, the edit-modal round-trip (open → empty prefill → set MONTHLY → save → badge appears → re-open shows MONTHLY prefilled → clear → badge gone), and — with `BrowserContext.setOffline(true)`, after confirming the service worker actually controlled the page first — checking a recurring item off while offline: it stayed in the active section with its badge (not moved to "done"), confirming `applyRecurrenceCompletionOffline` fired. No console errors in any of it.
- **Automatic price & image lookup**: when an item is created, or its `url` changes, `internal/handlers.scrapeProductInfo` (`internal/handlers/scrape.go`) kicks off a detached goroutine — never `r.Context()`, which is canceled the moment the response is written — that calls `internal/scraper.FetchProductInfo(ctx, url)` **once** and writes back whichever of `price`/`image_url` the item is still missing (an explicit `price` in the request only skips price detection — the image is still looked up) via `db.UpdateItemPriceIfMissing`/`db.UpdateItemImageIfMissing`. Both of those methods' `WHERE ... AND url = ? AND <column> IS NULL` guards are what makes this safe to run detached from the request: a slow scrape can never clobber a price/image the user already has, or one for a since-changed `url`, no matter how the race resolves. Unlike a purely fire-and-forget design, `handleItemsCreate`/`Update`/`Patch` (`internal/handlers/items.go`) each wait up to `syncScrapeTimeout` (2.5s) on that goroutine before responding: if it lands in time, the response already carries the real `price`/`image_url` and a response-only `price_status: "found"` field (`models.Item.PriceStatus`, `json:"price_status,omitempty"`, never persisted, never present on a plain `GET`); if not and a price was still needed, `scrapeProductInfo` returns `"pending"` and the goroutine keeps running in the background exactly as the original fully-async design always did — but if only the image was still outstanding (price was already known), it returns `"found"` instead, since price detection genuinely isn't pending; the image is treated as a best-effort enhancement that simply shows up next time the item is fetched if it's slow to arrive. `price_status` is `"none"` when there was nothing to scrape (no `url`, or no price found within the wait) — this is unaffected by whether an image was found. This bounded wait is what lets `static/js/list_view.js` update the DOM straight from the POST/PATCH response instead of reloading or polling — see the frontend bullet below — while still never blocking indefinitely on a slow or unresponsive site. `items.price_auto` (`INTEGER NOT NULL DEFAULT 0`, same 0/1-boolean convention as `done`) records whether the current `price` came from this lookup rather than a user — the frontend shows a small badge next to an auto-detected price, clickable to open the same edit-item modal used everywhere else; sending `price` in any request (including an explicit `null`) always resets it to `false`, since a manual value was supplied. `items.image_url` (`TEXT`, nullable) is scraper-only — there is no request field to set it manually — and is cleared back to `nil` whenever `url` changes to something new (both `PUT` and `PATCH` in `internal/handlers/items.go` compute this before calling `db.UpdateItem`), since an image found for the previous `url` no longer describes the item. `internal/scraper.FetchProductInfo` parses the page once (`extractProductInfo`) and looks for a price, an image, and a title (the last currently extracted but not yet consumed by any caller — see below) using `golang.org/x/net/html` rather than regex-scraping raw HTML. The request carries a real desktop-Chrome `User-Agent` and an `Accept-Language: fr-FR,fr;q=0.9,en-US;q=0.8,en;q=0.7` header (`internal/scraper/scraper.go`'s `userAgent`/`acceptLanguage` consts) — several storefronts this targets (Reichelt and Amazon in particular) block or serve a stripped page to a generic/library User-Agent, and this is purely to see the same page an ordinary visitor's browser would for a URL the user themselves added to their own list. Price priority order: Schema.org JSON-LD (`price` inside an `Offer`), OpenGraph/`product:` meta tags (`og:price:amount`/`product:price:amount`), a Twitter Card `twitter:dataN` value whose paired `twitter:labelN` reads as "Price"/"Prix" (`resolveTwitterPrice` — some storefronts expose a price only this way), then classic microdata (`itemprop="price"`, read off either a `content` attribute or the element's text — checked on every element, including a `<meta itemprop="price" content="...">`, not just non-`<meta>` tags). Image priority order: on an Amazon host (`isAmazonHost` — `amazon.*`, `amzn.to`, `amzn.eu`) only, Amazon's own product-image element (`id="landingImage"`/`id="imgBlkFront"`)'s `data-old-hires` or `data-a-dynamic-image` attribute first (`amazonLandingImage`/`bestDynamicImage` — Amazon's own `og:image` is frequently the site logo or a nav icon, never trusted first there); then, on every host, Schema.org JSON-LD (`image`, as a string, array, or nested `ImageObject`), `og:image` (skipped when the URL contains an Amazon-chrome term — `amazon-logo`/`nav2`/`favicon`/`tile` — `isAmazonGenericImage`, checked regardless of host but only ever matches on Amazon's own asset URLs in practice), classic microdata (`itemprop="image"`, off a `content`/`src` attribute), and the Twitter Card tag (`twitter:image`) as a last resort. Title priority order: JSON-LD (`name`, preferring an object whose own `@type` is `"Product"` over an unrelated `name` elsewhere on the page — `findJSONTitle`/`searchJSONProductName` — a page frequently carries more than one JSON-LD block), `og:title`, then the page's own `<title>`. None of this changes how a price is *stored* or *displayed*: `ProductInfo.Price` stays a bare `float64` with no currency symbol, exactly as before — `parsePriceString` just has to parse the number correctly (it already tolerates both `"15,90 €"`-style and `"15.90"`-style input), and the frontend's existing hardcoded EUR `Intl.NumberFormat` (`static/js/list_view.js`'s `euroFormatter`) renders it with a currency symbol on display as it always has. A relative image URL is resolved to absolute against the page's own URL (`resolveImageURL`, `url.ResolveReference`) and only accepted if the result is `http`/`https` with a non-empty host — the same rule `internal/validate.URL` applies to `item.url` — so a `javascript:`/`data:` URI can never end up as an item's `image_url`. Because this makes the server fetch an arbitrary URL a user typed into the `url` field, its `http.Client`'s `Transport.DialContext` is replaced with an SSRF guard (`safeDialContext` in `internal/scraper/scraper.go`) that resolves the host itself and only ever dials a public IP (rejecting loopback/private/link-local/multicast, which also covers the `169.254.169.254` cloud metadata address) — dialing the checked IP literal directly, rather than letting the transport re-resolve the hostname, is what closes the DNS-rebinding TOCTOU gap a naive "check then dial" would leave open. Every failure mode (network error, timeout, blocked host, no recognizable price/image/title markup) is treated identically as "nothing found" — this feature must never fail item creation/update outright, and the bounded wait keeps it from blocking indefinitely either.
- **Authentication & authorization**: local email+password (bcrypt) and generic OIDC/OAuth2 SSO (Authelia/Authentik/Keycloak/Google, ...) both establish the same opaque, server-side session — an HTTP-only, `SameSite=Lax` cookie whose value hashes to a row in the `sessions` table (`internal/auth.Service`); see the CSRF bullet below for why Lax is still safe here, and the "Mobile/PWA login loop" bullet in the handoff log for why it isn't `Strict`. Every `/api/v1/...` request is gated as a whole behind `(*handlers.Application).RequireSession` (wrapped once around a sub-`http.ServeMux` in `Routes()`, not per-route) — deliberately **not** a separate `internal/middleware` package, since that middleware needs `*db.DB` and `internal/handlers` needs to call it to build routes, which would be an import cycle either direction; it lives as an `*Application` method next to every other handler instead. `/auth/...` (login page, register/login/logout submit, OIDC redirect/callback) and all static assets stay unauthenticated by design — gating static files would break the service worker's cache-first app-shell strategy, so the auth boundary is the JSON API, and `static/js/app.js`'s `apiRequest()` handles the client-side redirect-to-login on a `401`. A house's lists/items are only visible to its members (`internal/handlers/rbac.go`'s `authorizeHouseAccess`/`authorizeHouseOwner`, checked against `house_members` via `internal/db`); `CreateHouseWithOwner` (the only way a house gets created through normal use) is the one place in `internal/db` that opens an explicit `sql.Tx`, so a house is never left without an owner. The OIDC client (`internal/auth/oidc.go`, `jwt.go`) is hand-rolled — discovery, Authorization Code + PKCE, JWKS fetch/cache, RS256 id_token signature verification — using only the standard library plus `golang.org/x/crypto/bcrypt` (the one new dependency this feature added), rather than pulling in `coreos/go-oidc`/`golang.org/x/oauth2`, per the "standard library wherever possible" convention below.
- **Docker build is multi-stage and CGO-free by requirement**: builder stage `golang:1.27.0-alpine` compiles `./cmd/server` with `CGO_ENABLED=0` to a static, stripped binary (`-ldflags="-s -w"`); runtime stage is `gcr.io/distroless/static-debian12` (no shell, no package manager — chosen over an Alpine runtime specifically to shrink the attack surface, since this binary needs nothing beyond CA certificates, which that image already ships), running as a fixed non-root numeric UID:GID (`10001:10001`), chosen specifically so it maps cleanly under Podman's rootless user-namespace remapping — don't switch to a named `USER` (or to distroless's own built-in `nonroot` user, which is UID `65532`) without keeping the numeric UID pinned. Because distroless has no shell to run `adduser`/`mkdir` in, the `/etc/passwd`/`/etc/group` entries for that UID and an empty, correctly-owned `/data` directory are created in the *builder* stage and `COPY --from=build`'d into the runtime stage, alongside `static/` and `templates/` (the latter for `login.html`). Distroless ships no `tzdata`; this is a non-issue since the app never calls `time.LoadLocation` and every timestamp it stores or logs is UTC by convention, and `compose.yml` no longer sets `TZ` (it would be silently ignored). `compose.yml`'s `trakka` service also sets `read_only: true` (with `/data` and a small `tmpfs` at `/tmp` as the only writable paths) and `cap_drop: [ALL]` — a plain HTTP+SQLite server needs no Linux capabilities, not even `NET_BIND_SERVICE`, since it binds an unprivileged port.
- **`compose.yml`** must stay valid for both `docker compose` and `podman-compose` — avoid Compose fields that only one of the two supports. The `radicale` (CalDAV) service is optional and gated behind the `calendar` profile; it should never start by default.
- **PWA/offline (`static/sw.js`, `static/js/db.js`)**: full design in [docs/PWA.md](docs/PWA.md) — read it before touching either file. The short version: `db.js` is loaded both as a page `<script>` and via the service worker's `importScripts()`, so it must never reference `window`/`document`, only `indexedDB` and `self`. `sw.js` transparently intercepts every `fetch()` from `app.js`; offline mutations get a temporary `temp-list-*`/`temp-item-*` id and are queued in IndexedDB, and `remapTempId()` patches any still-queued request that depended on one once it gets a real server id. `app.js` also reads (never writes) `TrakkaDB.getQueue()` directly, purely to show the "N en attente" badge — the service worker remains the only writer of the queue/mirror stores; don't add a second writer. Houses get the same read-only mirror (`TrakkaDB.getHouses`/`putHouses`/etc.) but are deliberately **not** offline-writable — creating/renaming/deleting a house while offline returns a `503` rather than being queued, because queueing it would require a second level of temp-id chaining (list → house, on top of the existing item → list) that the offline design explicitly doesn't support; see "Houses are read-mirrored but not offline-writable" in [docs/PWA.md](docs/PWA.md). Session cookies are ambient/browser-managed, so a queued write replayed later by `flushQueue()` automatically carries whatever cookie is present at replay time — no header snapshotting needed — but `flushQueue()` special-cases a `401` reply (expired session) by leaving the entry queued instead of discarding it like an ordinary rejected `4xx`, so a session expiring mid-queue can't silently drop an offline edit; see "Session expiry vs. the offline queue" in [docs/PWA.md](docs/PWA.md). `/auth/...` requests bypass the service worker entirely (checked first in the `fetch` handler) — never cache-first-served, never offline-queued. **Service workers require a secure context** (`https://` or exactly `http://localhost`) — they silently don't register anywhere else, which is why `compose.yml` serving plain HTTP is only enough for `localhost` testing (see [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)).

## Security rules

These are standing constraints for this codebase, not just how the current code happens to be written — preserve them in anything new:

- **SQL**: every query goes through `database/sql` parameter placeholders (`?`), exclusively inside `internal/db`. Never build a query string by concatenating or `fmt.Sprintf`-ing a user-supplied value into it.
- **URLs**: any user-supplied URL (currently: an item's `url` field) must pass `internal/validate.URL`, which requires an absolute `http://` or `https://` URL with a non-empty host — this blocks `javascript:`, `data:`, and similar schemes from ever being persisted. Apply the same validator to any future field that accepts a URL. The frontend (`static/js/app.js`) re-checks the scheme client-side before ever setting an `<a href>`, as defense in depth — keep that check if you touch the rendering code.
- **Response encoding**: JSON responses rely on `encoding/json`'s default `SetEscapeHTML(true)` behavior (escaping `<`, `>`, `&`) — never call `SetEscapeHTML(false)` on the encoder in `internal/handlers/json.go`.
- **Frontend DOM**: `static/js/app.js` builds all dynamic content with `textContent`/`createElement`, never `innerHTML` with interpolated data. Keep that pattern for any new UI code.
- **HTTP security headers**: `internal/handlers.SecurityHeaders` sets `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, a `Content-Security-Policy`, and `Referrer-Policy: no-referrer` on every response, including static files. The CSP has two deliberate exceptions: `script-src` trusts `https://cdn.tailwindcss.com` (the Tailwind Play CDN script `static/index.html` and `templates/login.html` both load), and `style-src` allows `'unsafe-inline'` because that script generates utility CSS into a `<style>` tag at runtime rather than an external file — there was no way to use Play CDN without it; and `img-src` trusts any `http:`/`https:` source (not just `'self' data:'`) because an item's scraped product thumbnail (`items.image_url`, see the price & image lookup bullet above) can point at any storefront's CDN, with no fixed host list to name in advance — the same reasoning that already lets `item.url` link out anywhere, just extended to `<img src>` now that thumbnails render inline instead of only linking out. Everything else stays strict: no `'unsafe-eval'`, no other inline scripts, no other allowlisted hosts. If you add any other inline `<script>`/`<style>`, move it to an external file instead of widening this further. If Play CDN's ~400KB runtime cost ever needs to go away, switch to a precompiled Tailwind build (Tailwind CLI, output to `static/js/` or a new `static/css/`) instead of the CDN script — that would let `style-src` drop `'unsafe-inline'` again. The OIDC login flow is entirely server-mediated redirects (never a page-JS `fetch` to the IdP), so none of this needed loosening for OIDC support.
- **Passwords & sessions**: passwords are hashed with `golang.org/x/crypto/bcrypt` (`internal/auth.HashPassword`/`VerifyPassword`) — never store or log a plaintext password. Session tokens are `crypto/rand`-generated and stored **hashed** (`internal/auth.HashToken`, SHA-256) in the `sessions` table — the raw token exists only in the cookie and in-flight, so a database leak alone can't hand out live sessions. `Authenticate()` runs a dummy bcrypt comparison even on an unknown email/OIDC-only account, so failed-login timing doesn't reveal whether an email is registered.
- **CSRF**: the session cookie is `SameSite=Lax` (changed from `Strict` — see the mobile-login-loop fix below), which is still the app's CSRF defense for `/api/v1/...`: Lax omits the cookie from every cross-site request except a top-level GET navigation, and it's never attached to a cross-site `fetch`/XHR (the only way this SPA issues `POST`/`PUT`/`PATCH`/`DELETE`) or a cross-site form POST. Every `GET` route this app exposes is a pure read (`internal/handlers/app.go` — no route mutates state on `GET`), so the one thing Lax additionally allows over Strict — a cookie attached to a cross-site top-level GET navigation — cannot be turned into a state change. `/auth/login`/`/auth/register` deliberately carry no separate CSRF token: there's no pre-existing session for a forged POST to hijack at that point, so a token there would add no real protection. Don't loosen this further (e.g. to `SameSite=None`) without adding an equivalent CSRF defense first, and don't add a state-mutating `GET` handler without revisiting this reasoning.
- **OIDC id_token verification**: hand-rolled in `internal/auth/jwt.go`/`oidc.go` — hard-rejects any `alg` other than `RS256` *before* any key lookup (alg-confusion defense), verifies the RSA signature before ever trusting the claims, and checks `iss`/`aud`/`exp`/`iat` afterward. If you touch this code, preserve that ordering; don't reorder "check claims" before "verify signature".
- **SSRF (server-side request forgery)**: `internal/scraper.FetchPrice` is the one place this server makes an outbound HTTP request to a URL a user supplied (an item's `url` field) rather than one hard-coded at deploy time. It's the same category of risk as a Slack/Discord link-unfurler, and is defended the same way: `internal/scraper`'s `http.Client` uses a custom `Transport.DialContext` (`safeDialContext`) that resolves the target host itself and only ever connects to a public IP — rejecting loopback, private/ULA ranges, link-local (including the `169.254.169.254` cloud metadata address), multicast, and unspecified addresses — and dials that checked IP literal directly rather than letting the transport re-resolve the hostname, closing the DNS-rebinding TOCTOU gap a "resolve, check, then dial the hostname" approach would leave open. Any future feature that makes the server fetch a user-supplied URL must reuse this guard (or an equivalent one) rather than calling `http.Get`/`http.Client.Do` directly against untrusted input.

## CI/CD & Sécurité

`.github/workflows/ci.yml` is Trakka's GitHub Actions pipeline. It runs on every push to `main`, every `v*.*.*` tag, every pull request targeting `main`, every published GitHub Release, and on-demand via `workflow_dispatch` (with `image_tag`/`push_to_ghcr` inputs for manual/nightly-style builds). Superseded runs on the same branch/PR are cancelled automatically (`concurrency`).

### Pipeline shape

Four jobs, the last depending on the first three (`needs: [lint, test, security-scan]`):

1. **`lint`** — `gofmt -l .`, `go vet ./...`, `go mod verify`, then `golangci-lint run` (via `golangci-lint-action`, version pinned through the `GOLANGCI_LINT_VERSION` workflow env var). `go mod verify` confirms every module in the build matches the checksums recorded in `go.sum`, so a tampered or corrupted dependency can never silently compile in.
2. **`test`** — `go test -v -race -cover ./...`. The race detector needs `CGO_ENABLED=1` (it links a small C runtime) — this is set for this one step only and has no bearing on the shipped binary, which the `Dockerfile` still always builds with `CGO_ENABLED=0` (see "Go version pinning is intentional and fragile" above; `modernc.org/sqlite` is pure Go either way, so enabling cgo here changes nothing about which SQLite driver gets used).
3. **`security-scan`** — `govulncheck ./...` (native to the Go ecosystem, maintained by the Go team; flags only vulnerabilities your code actually reaches, not every CVE sitting in an unused transitive dependency), `gosec` (Go SAST, SARIF output), a Trivy filesystem/dependency scan (`go.sum` CVEs, SARIF output), and `gitleaks detect` (secret scanning, SARIF output) — checked out with `fetch-depth: 0` specifically for this job (every other job uses the default shallow clone), since gitleaks scans the *full commit history*, not just the tree at HEAD: a credential removed in a later commit is still compromised for good, so only ever scanning the current checkout would miss exactly the case this tool exists for. All three SARIF files upload to the repo's **Security → Code scanning** tab (`github/codeql-action/upload-sarif`, tagged with a `category` — `gosec` / `trivy-fs` / `gitleaks` — so the tools' findings don't overwrite each other there) and this job needs `security-events: write` to do that. `gitleaks` is installed via `go install github.com/zricethezav/gitleaks/v8@${GITLEAKS_VERSION}` (an exact pinned version, same convention as `govulncheck`/`gosec` below) rather than a marketplace Action, and run with `--redact` so a matched secret's actual value never appears in the job log or the uploaded SARIF. A local, opt-in pre-commit hook (`.githooks/pre-commit`, enabled via `git config core.hooksPath .githooks`) runs `gitleaks protect --staged` for faster feedback before a push — see [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md#preventing-committed-secrets) — but CI is what actually gates a merge; the hook is a convenience, not the enforcement point, and silently no-ops (rather than blocking) if gitleaks isn't installed locally.
4. **`build-scan-push`** — builds the Docker image (linux/amd64 only, loaded locally) purely to run a Trivy **image** scan (SARIF, `category: trivy-image`) before anything is pushed anywhere; only if that passes does it build the real multi-arch (linux/amd64+arm64) image and push it to `ghcr.io` — and only when the run isn't a pull request (a PR gets the build+scan but never the push; see the `policy` step's logic) — then attests build provenance for the pushed image via `actions/attest-build-provenance` (a GitHub-native SLSA attestation, keyless via OIDC — hence this job's `id-token: write`/`attestations: write` — verifiable later with `gh attestation verify`). Tags follow `docker/metadata-action`: `latest` and `sha-<short>` on `main`, the tag's own semver on a `v*.*.*` push, plus the manual `image_tag` input on a `workflow_dispatch` run.

Both Trivy scans and gosec are hard gates (`exit-code: "1"` on any unfixed `CRITICAL`/`HIGH` finding) — they still upload whatever SARIF they produced first (`if: always()` on the upload step), so a failing run still shows its findings in the Security tab rather than only in the job log.

### Permissions model

The workflow denies everything by default (`permissions: read-all` at the top level) and each job then declares only what it actually needs, narrowing below even that default:

| Job | Permissions | Why |
|---|---|---|
| `lint` | `contents: read` | just needs to check out the code |
| `test` | `contents: read` | same |
| `security-scan` | `contents: read`, `security-events: write`, `actions: read` | the second grant is for uploading SARIF; the third is required by `upload-sarif` itself to look up the current workflow run (only actually enforced on a private repo, but harmless to grant regardless) |
| `build-scan-push` | `contents: read`, `packages: write`, `security-events: write`, `actions: read`, `id-token: write`, `attestations: write` | pushing to GHCR, uploading the image-scan SARIF, minting the provenance attestation, and (same as `security-scan`) letting `upload-sarif` read the current workflow run each need their own scope |

No job gets `write` access it doesn't use — in particular, only `build-scan-push` can ever push anything, and only on a non-PR event.

**Gotcha discovered the hard way**: `github/codeql-action/upload-sarif` calls the GitHub REST API to look up the current workflow run (used to correlate the SARIF upload with it) — this needs `actions: read`, which is *not* covered by `security-events: write`. On this originally-private repo, omitting it made every `upload-sarif` step fail with `Error: Resource not accessible by integration - .../actions/workflow-runs#get-a-workflow-run`, and because the *next* step in `security-scan` (`Trivy filesystem scan`) had no `if: always()`, that failure skipped it too — so the following `Upload Trivy filesystem SARIF` step (which *does* have `if: always()`) then failed a second, different way: `Error: Path does not exist: trivy-fs-results.sarif`, because the scan that would have produced it never ran. Both symptoms had the single root cause above. Any job that calls `upload-sarif` needs `actions: read` alongside `security-events: write`, even though GitHub's own quickstart examples often show only the latter.

### The golden rule: pin every action to a commit SHA

Every `uses:` in this workflow is pinned to a full 40-character commit SHA, never a floating tag (`@v4`) or branch — a tag can be force-moved to point at different code (the exact "GitHub Action supply-chain poisoning" class of attack this defends against), a SHA cannot. **Any new action added to this workflow, or a version bump of an existing one, must follow the same rule**: resolve the release tag to its commit SHA and pin that, with a `# vX.Y.Z` trailing comment so a human can still tell which release it is at a glance:

```yaml
uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
```

To find the SHA for a tag: `git ls-remote --tags https://github.com/<owner>/<repo>.git <tag>` locally, or `gh api repos/<owner>/<repo>/tags` (the *tags* endpoint, not `git/refs/tags`, which can return the wrong SHA for an annotated tag — the tags endpoint always returns the dereferenced commit; this actually happened while pinning this workflow: `golangci-lint-action`, `trivy-action`, and `codeql-action`'s tags are annotated, and `git/refs/tags` returned each tag object's own SHA rather than the commit it points to). The GitHub web UI's release page also shows the tag, which you can resolve the same way. Never trust a SHA pasted from anywhere you can't independently re-derive.

### Running the same checks locally

```bash
gofmt -l .                                                  # must print nothing
go vet ./...
go mod verify
go build -o trakka ./cmd/server

golangci-lint run ./...                                     # config: .golangci.yml
CGO_ENABLED=1 go test -v -race -cover ./...                 # -race needs a C toolchain (gcc/clang); CI enables cgo for this step only

go install golang.org/x/vuln/cmd/govulncheck@v1.7.0         # match GOVULNCHECK_VERSION in ci.yml
govulncheck ./...

go install github.com/securego/gosec/v2/cmd/gosec@v2.29.0   # match GOSEC_VERSION in ci.yml
gosec ./...

go install github.com/zricethezav/gitleaks/v8@v8.28.0       # match GITLEAKS_VERSION in ci.yml
gitleaks detect --source . --redact                         # secret scan (full git history)

# Trivy (needs the trivy CLI installed separately, e.g. via your package
# manager or https://aquasecurity.github.io/trivy/):
trivy fs --severity CRITICAL,HIGH --ignore-unfixed .
docker build -t trakka:scan . && trivy image --severity CRITICAL,HIGH --ignore-unfixed trakka:scan
```

`.golangci.yml` configures `errcheck` to exclude the `Close`/`Rollback` deferred-cleanup pattern already used throughout `internal/db` (`defer rows.Close()`, `defer tx.Rollback()`, etc.) — checking the return value of a cleanup call made *after* the operation that actually mattered (the query, the write, the commit) already returned and was checked is a near-universal, low-risk Go idiom that most real-world `errcheck` configs exclude the same way; without it, turning on `golangci-lint` here would fail on pre-existing, harmless findings unrelated to whatever a given PR actually changed. A handful of `#nosec G101`/`#nosec G124` comments in `internal/settings/settings.go`, `internal/handlers/auth.go`, and `internal/auth/service.go` document specific gosec false positives the same way (a settings-table key name and a map of UI strings that only *look* like credentials to a naive string-matcher; and four `http.Cookie` literals gosec's G124 check can't recognize as secure because their `Secure` field is a variable — `s.CookieSecure`/`app.Auth.CookieSecure`, wired from `SESSION_COOKIE_SECURE` — rather than a literal `true`). Don't add a new blanket exclusion without the same kind of one-line justification these carry; a real finding should get fixed, not silenced.

## Contribution & GitHub templates

`.github/` carries the standard GitHub community/governance files, in English — like the rest of code, comments, and technical documentation per the "Documentation" convention below (only end-user-visible UI strings are French):

- **`.github/PULL_REQUEST_TEMPLATE.md`** — auto-filled into every new PR's description. Structured as: a free-form summary, a checkbox "type de changement" (Fix/Feature/Refactoring/Docs/CI-CD), a linked-issue field (`Fixes #123`), and a validation checklist (`go build`/`go vet`/`gofmt`/`go test`/`golangci-lint` all clean, `CLAUDE.md` updated if the PR changes a convention/security rule/architecture it documents, PWA/offline behavior manually checked if `static/js/*.js` or `static/sw.js` changed). **When opening or reviewing a PR, actually work through this checklist rather than leaving it as inert boilerplate** — it mirrors exactly what `lint`/`test`/`security-scan` already enforce in CI (see "CI/CD & Sécurité" above), so a PR that fails it will also fail CI.
- **`.github/ISSUE_TEMPLATE/`** — GitHub Issue Forms (YAML, not the older Markdown template format): `bug_report.yml` (description, repro steps, expected behavior, Go/JS logs, environment, browser) and `feature_request.yml` (problem, proposed solution, alternatives considered, a multi-select for which area it touches — lists/budget/sharing/PWA/auth/other — plus a checkbox nudging proposals to stay consistent with the project's "ultra-lightweight, stdlib-first" goal). `config.yml` disables blank issues and points contributors at `CLAUDE.md` and `CONTRIBUTING.md` before they file a ticket.
- **`.github/CONTRIBUTING.md`** — the short contributor workflow (fork → branch → respect project conventions → local checks → PR against `main` using the template above → CI must pass) that links back to this file for anything substantive rather than duplicating it.
- **`.github/CODE_OF_CONDUCT.md`** — the standard Contributor Covenant v2.1, with the enforcement contact set to the project's maintainer email.

None of this changes any build/runtime behavior — it's pure repository governance, kept in `.github/` alongside `workflows/ci.yml` per GitHub's own convention for where these files are discovered.

## Project status and session handoff (last updated: 2026-08-30, "pinning: dashboard dynamic refresh + House-visible Space pinning": pin/unpin now reflects on the dashboard without a page reload, and a Space visible only through House membership — not owned, not explicitly shared — can now be discovered and pinned too)

This section is a running log for you (the user) and Claude Code across sessions. It reflects the observed state of the repo as of the date above (build/vet/gofmt verified). Update it at the end of every significant work session rather than letting it drift.

### What's done

- **Complete backend**: `cmd/server`, `internal/config`, `internal/models`, `internal/db` (houses, lists, items, users, sessions, house_members, schema, errors), `internal/auth` (password hashing, session tokens, hand-rolled OIDC client), `internal/handlers` (houses, house_members, lists, items, health, middleware, auth_middleware, auth, rbac, json, scrape, recurrence), `internal/validate`, `internal/scraper` (background price lookup). Builds, `go vet`, and `gofmt -l` are clean.
- **Automatic price & image lookup**: creating an item (or changing an existing one's `url`) kicks off a best-effort fetch of that URL, in a single request, looking for a price (OpenGraph, JSON-LD, or `itemprop="price"` microdata — skipped only if `price` was given explicitly) and a product image (OpenGraph `og:image`, JSON-LD `image`, or `twitter:image`), writing back `items.price_auto = 1`/`items.image_url` for whichever was found — see the Architecture section above for the full design (the `UpdateItemPriceIfMissing`/`UpdateItemImageIfMissing` race guards, the SSRF-hardened `http.Client` in `internal/scraper`, the bounded `syncScrapeTimeout` wait, why a slow lookup still can't block the request indefinitely). The frontend (`static/js/list_view.js`) updates the item row and the finance summary bar directly from the create/update/patch response — no `location.reload()` anywhere in the app — showing a small clickable badge next to an auto-detected price, a pulsing "Détection du prix…" badge while `price_status: "pending"` (with a single delayed re-fetch via `scheduleAutoPriceRefresh` to pick the result up), a "Prix en attente de synchro" badge for an item still sitting in the offline sync queue (`isOfflineQueuedItem`, cleared automatically once `sw.js`'s `trakka-sync-complete` message triggers a full list refetch), and — when `image_url` is present — a 48px `buildItemThumbnail` clickable/hoverable thumbnail that opens a lightbox modal (`#image-preview-modal`) rather than navigating; no space is reserved when there's no image. Verified: unit tests for the HTML/JSON-LD/microdata price and image extraction, including relative-to-absolute image URL resolution and unsafe-scheme rejection (`internal/scraper/scraper_test.go`), a DB-layer test for both three-way race guards (`internal/db/items_test.go`), `go build`/`go vet`/`gofmt` clean — but **not** against a real e-commerce site with actual price/image markup (no reliable, stable public test fixture was available in the sandbox this was built in; `example.com` has neither, so only the "nothing found" path and the SSRF rejection of `127.0.0.1` were exercised live), and none of the frontend (thumbnail rendering, hover-zoom, lightbox modal, bounded-wait/pending-badge behavior) has been exercised in a browser yet. Try it against a real product page (a Shopify store, an Amazon/Fnac/Darty listing, etc.) at least once before relying on it.
- **Scraper hardening for real-world storefronts (Amazon, Reichelt)**: `internal/scraper` sends a real desktop-Chrome `User-Agent` and `Accept-Language: fr-FR,fr;q=0.9,en-US;q=0.8,en;q=0.7` instead of the old `TrakkaBot/1.0` identifying string, since several storefronts blocked or served a stripped no-price page to it. Amazon hosts (`isAmazonHost`) get dedicated image handling: `#landingImage`/`#imgBlkFront`'s `data-old-hires`/`data-a-dynamic-image` attributes (the real product photo) are tried before anything else, and `og:image` — which on Amazon is frequently the site logo or a nav icon rather than the product — is only used as a fallback, and rejected outright if its URL contains an Amazon-chrome term (`amazon-logo`/`nav2`/`favicon`/`tile`). Price extraction now checks JSON-LD before meta tags (previously the reverse) and gained a Twitter Card `twitter:dataN`/`twitter:labelN`-pair fallback (some storefronts, Reichelt among them, expose a price only that way); a latent bug where `<meta itemprop="price" content="...">`-style microdata was never actually checked (the itemprop check only ran for non-`<meta>` elements) is fixed by checking `itemprop` on every element regardless of tag. Image extraction gained an `itemprop="image"` microdata source. `ProductInfo` gained a `Title` field (JSON-LD `name` preferring an actual `@type: "Product"` object, then `og:title`, then `<title>`) — extracted since it's free from the same fetch, but **not yet wired to anything downstream** (no handler/DB path uses it; `internal/handlers`/`internal/db` are unchanged by this session). None of this changes how a price is stored or displayed — `ProductInfo.Price` is still a bare `float64`, and the frontend's hardcoded EUR formatter still renders it with a currency symbol exactly as before; only the underlying extraction got more sources and better priority ordering. Full design in the "Automatic price & image lookup" bullet above. Verified: `internal/scraper/scraper_test.go` gained tests for the Amazon image path (`data-old-hires`, `data-a-dynamic-image` largest-wins, generic-term rejection, the non-generic og:image fallback), microdata image, meta-tag microdata price, the Twitter Card price fallback, the JSON-LD-over-OpenGraph price reordering, and all three title sources — `go build`/`go vet`/`gofmt -l .`/`go test ./...` all clean. **Not yet done**: still never exercised against a real Amazon or Reichelt product page (or any real site) — only synthetic HTML fixtures, same caveat the original price/image lookup feature already carried. Try it against a real Amazon and a real Reichelt listing at least once before relying on it.
- **Amazon-specific price selectors**: the generic OpenGraph/JSON-LD/microdata price chain frequently comes up empty on a real Amazon product page even after the scraper hardening above got the image working — Amazon's actual displayed price mostly lives in page-specific markup (`a-offscreen` spans, `priceblock_*` ids from older templates) that none of those generic sources capture, and its JSON-LD price, when present at all, is often stale relative to what's actually shown. `internal/scraper.resolveAmazonPrice` (`internal/scraper/scraper.go`) tries five Amazon-only sources, in priority order, before falling back to anything generic: (1) an `a-offscreen` span's text nested inside `apexPriceToPay`/`#corePrice_desktop` (Amazon's current "buy box" template), (2) an `a-offscreen` span nested inside any other `a-price` element (first one found), (3) `#priceblock_ourprice`/`#priceblock_dealprice`/`#priceblock_saleprice` (older template), (4) the `a-color-price` class, (5) `#price_inside_buybox`. Since `extractProductInfo`'s single tree-walk has no ancestor stack, `walk` now threads two booleans (`inAmazonPrimaryPrice`/`inAmazonAPrice`) down through each recursive call instead, set the moment a node itself matches `apexPriceToPay`/`#corePrice_desktop`/`a-price` and inherited by every descendant — this is what lets a nested `span.a-offscreen` know which priority tier it's in without a separate pass. Only on an Amazon host (`isAmazon`) are these five tried at all, and only *before* JSON-LD — the reverse of the generic (non-Amazon) chain's order, and a deliberate exception to it, for the reason above. If none of the five match, resolution falls through to the existing generic chain unchanged: JSON-LD (`searchJSONPrice`'s key list gained `highPrice` alongside the existing `price`/`priceAmount`/`lowPrice`, since some Amazon `AggregateOffer` blocks carry only a low/high range), then an Amazon `data-asin-price`/`data-price` element attribute (also collected during the same walk), then `og:price:amount`/`product:price:amount`, the Twitter Card fallback, and classic microdata — so a non-Amazon host's behavior, and every previously-passing test, is unchanged. As with every other price source in this file, `ProductInfo.Price` stays a bare `float64` with no currency symbol — `parsePriceString` already tolerates `"14,99 €"`-style text (it strips everything but digits/`,`/`.` before parsing), so no new cleaning logic was needed to get a correct numeric value out of Amazon's `"14,99&nbsp;€"`-shaped markup; the frontend's existing hardcoded-EUR formatter still owns turning the number back into a displayed price, per the documented reasoning in the "Automatic price & image lookup" bullet above. Verified: eleven new cases in `internal/scraper/scraper_test.go` (all five selectors individually, the Amazon-selectors-beat-JSON-LD priority case, the JSON-LD fallback when no selector matches, `highPrice`, and the `data-asin-price` fallback) plus every pre-existing test — `go build`/`go vet`/`gofmt -l .`/`go test ./...` all clean. **Not yet done**: same caveat as the scraper-hardening entry above — these selectors were written against the Amazon DOM structures described in the request that prompted them, not exercised against a real, live `amazon.*` page (no such page was available in the sandbox this was built in); confirm against a real Amazon listing before relying on it, and revisit if Amazon changes its markup (page-specific CSS-class scraping is inherently more fragile than the structured-data sources it now falls back to).
- **Amazon anti-blocking headers, JS-state price, and raw-body last resort**: a real report against a live Amazon URL (`amazon.fr/dp/...`) found the price still coming back empty even with the DOM selectors above in place — the two suspected causes are Amazon serving its CAPTCHA/"Server Busy" anti-bot challenge page instead of the product page, and/or a page variant that renders its price client-side from embedded JS state rather than in static HTML the selectors can see. Three changes address this, all in `internal/scraper/scraper.go`: (1) `setBrowserHeaders` (split out of `FetchProductInfo` specifically so it's unit-testable against a bare `*http.Request` — the SSRF guard refuses to let a test actually fetch from a loopback `httptest` server) now sets a full realistic Chrome navigation fingerprint on every request, not just `User-Agent`/`Accept`/`Accept-Language` as before: `Device-Memory`, `Sec-Ch-Ua`/`Sec-Ch-Ua-Mobile`/`Sec-Ch-Ua-Platform` (Client Hints), and `Sec-Fetch-Dest`/`Mode`/`Site`/`User` (Fetch Metadata) — `userAgent`'s Chrome version was bumped to `124` to match `secChUa`'s, since a mismatched pair is itself a bot signal some anti-bot checks look for. (2) `amazonScriptStatePrice`, tried only on an Amazon host and only after every DOM/JSON-LD/data-attribute source already tried above comes up empty: it scans the raw text of *every* `<script>` tag on the page (not just `application/ld+json` ones, which is all `ldjsonBlocks` ever collected) for a `priceAmount`/`displayPrice`/`buyingPrice`/`price` key immediately followed by a number — covering an Amazon widget payload like `<script type="a-state" data-a-state='{"key":"..."}'>{"priceAmount":14.99,...}</script>`, whose price would otherwise be invisible to every structured source above since it's inline JS state, not markup. (3) `amazonRawBodyPrice` is the absolute last resort — tried only on an Amazon host and only if literally everything else (selectors, JSON-LD, data-attribute, script-state, and even the generic OpenGraph/Twitter/microdata chain) found nothing at all: three direct regexes (`"priceAmount":\s*([0-9]+\.?[0-9]*)`, `"price":\s*"([0-9]+[.,][0-9]{2})"`, `class="a-offscreen">([0-9]+[.,][0-9]{2})\s*€`) run against the raw, unparsed response body — this is why `extractProductInfo` now reads its input fully into a `[]byte` up front (`io.ReadAll` then `html.Parse(bytes.NewReader(body))`) instead of streaming straight into `html.Parse`, so that raw byte slice is still available for this tier after DOM parsing. Separately, `FetchProductInfo` gained a `logger *slog.Logger` parameter (threaded from `app.Logger` at both call sites, `internal/handlers/scrape.go` and `internal/handlers/price_alerts.go`) purely to log one `Debug` line (`logIfAmazonBlocked`) when a fetched Amazon page's body contains `"Server Busy"` or `"Type the characters you see in this image"` — Amazon's own anti-bot challenge page text — since that failure mode otherwise looks identical to "genuinely no price on this page" from every caller's point of view, and being able to tell the two apart in the logs is the whole point of the addition; `logger` may be `nil` (checked before use) since not every future caller necessarily has one. None of this touches how a price is stored/displayed — every new source still ultimately funnels through `parsePriceString`, and `ProductInfo.Price` is still a bare `float64` — see the "Amazon-specific price selectors" bullet above and "Automatic price & image lookup" further above for that reasoning, which is unchanged. Verified: 13 new test cases in `internal/scraper/scraper_test.go` (`setBrowserHeaders`' full header set including the UA/Client-Hints version match, the script-state fallback via two different script shapes and its non-Amazon-host no-op, the raw-body fallback via an HTML-comment-hidden price its non-Amazon-host no-op, a priority-ordering test confirming the DOM selector still wins when all three tiers could match, and `logIfAmazonBlocked`'s four cases — logs on each marker, silent on an ordinary page, silent on a non-Amazon host, and a nil-logger no-panic case) plus every pre-existing test in the package — `go build`/`go vet`/`gofmt -l .`/`go test ./...` all clean. **Not yet done**: still not exercised against the actual reported URL or any other live Amazon page (no network access in the sandbox this was built in) — the anti-bot headers and both new fallback tiers were written from the failure description and Amazon's known page structures, not confirmed against a real response; if the price is still empty after this, the next things to check against a real fetch are (a) whether Amazon's response body actually now differs from a blocked page (the new debug log should say so directly) and (b) capturing a real page's HTML to see whether the price lives somewhere none of these three tiers anticipated.
- **Complete REST v1 API** documented in [docs/API.md](docs/API.md): CRUD for `houses` (RBAC-scoped), `house_members` (roster + invite-by-email + remove), `lists` and `items`, filtering (including lists by `?house_id=`), partial `PATCH`, `/api/v1/me`, `/healthz`.
- **Authentication & authorization**: local email+password (bcrypt) and generic OIDC/OAuth2 (Authelia/Authentik/Keycloak/Google, ...) both establish an opaque, server-side session via HTTP-only `SameSite=Lax` cookie. Every `/api/v1/...` request requires a valid session (`RequireSession` middleware); `house_members` gives every house one or more members with a role (`owner`/`member`), and a house's lists/items are only visible to its members. New users get a personal "Ma Maison" house automatically. Login page at `templates/login.html` (`html/template`, the first server-rendered page in this app). See the Architecture section above for the full design (import-cycle reasoning for keeping this in `internal/handlers` rather than a separate `internal/middleware` package, the hand-rolled OIDC/JWT verification, the `sql.Tx` in `CreateHouseWithOwner`, the CSRF reasoning). Full manual smoke test (register/login/logout, RBAC isolation between two users, invite flow, wrong-password/duplicate-email handling) verified end-to-end via curl and a headless-Chromium (Playwright) browser script — no dedicated test file was added to the repo since there's no test tooling set up yet (see "What's left" below).
- **FR/EN multilingual UI**: `static/locales/fr.json`/`en.json` + `static/js/i18n.js` — see the "UI language" bullet in Project conventions below for the full mechanism. Covers the dashboard/header/modals/item-form static markup plus the network-status text, the "N en attente" badge, and the delete/remove-member `aria-label`s in JS. **Not yet covered**: `templates/login.html` (still French-only, server-rendered, never touches `i18n.js`), and several other JS-generated strings in `list_view.js`/`app.js` (item counts, "Terminés (N)", API error messages, empty-state text) — translating those would mean threading `t()` through most of the dynamic rendering code, which wasn't done this session. `sw.js`'s `APP_SHELL` caches `/js/i18n.js` and both locale JSON files (cache bumped to `v8` for this). Verified end-to-end in a real headless-Chrome session (register → create list/item → toggle FR/EN → confirm both static and dynamic strings update, no console errors).
- **Purchase planning / budget forecasting**: items can carry an optional `target_month` (`TEXT`, `YYYY-MM`, validated by `internal/validate.Month`, added to the `items` table the same `addColumnIfMissing`-guarded way as `price`/`image_url` — see the Architecture section above and [docs/DATABASE.md](docs/DATABASE.md)). Deliberately modeled on `items`, not `lists`, since the goal is scheduling individual planned purchases, not whole lists. `POST`/`PUT`/`PATCH /api/v1/items` all accept it (see [docs/API.md](docs/API.md#post-apiv1items)); `PATCH` follows the same "absent = untouched, empty string = clear" convention already used for `url`. The frontend adds a second dashboard tab, "Budget & Prévisions Achats" (`static/js/planning.js`, `#tab-planning`/`#planning-view` in `index.html`), scoped to the current house like the existing lists tab: it fetches every shopping list's items (the same `?house_id=` + per-list fan-out `loadDashboard` already does), keeps only the ones with a `target_month` set, and groups them by month over a selectable 3/6/12-month horizon (the current calendar month always counts as the first of "the next N", not skipped), showing a running total and a per-month subtotal. Each item row has a `<select>` (options: unschedule, or any of the next 12 months regardless of the active horizon filter) that `PATCH`es the item's `target_month` immediately — this is the "report a purchase to another month" UI the feature was built for. Scheduling an item in the first place happens from the ordinary item create/edit UI (`static/js/list_view.js`): a shopping-list-only (`data-shopping-only`) `<input type="month">` next to the price field, plus a small violet "target month" badge on the item's row in the normal list view once set. Fully i18n'd (horizon labels, month names via `Intl.DateTimeFormat` in the active UI language, totals) and integrated with the offline-queue/house-switch/language-switch refresh hooks already in `app.js`. `sw.js`'s `APP_SHELL` now also caches `/js/planning.js` (cache bumped to `v9`). Verified end-to-end in two real headless-Chrome sessions: create-with-month via the item form, badge rendering, tab switching, 3/6/12 horizon filtering, correct per-month/total sums, rescheduling and unscheduling via the `<select>`, and the edit-modal round-trip (open → prefilled month → change → save → re-open shows the new value) — no console errors in any of it. **Not yet done**: no Go unit test for `internal/validate.Month` (the existing `internal/validate` package has none for `URL` either, so this matches current — imperfect — convention rather than a regression) and no dedicated `internal/db`/`internal/handlers` test for `target_month`, only the live browser/curl verification above.
- **Recurring items**: an item can carry `recurrence_rule`/`due_date`/`recurrence_end_date` (`items.is_recurring` is derived, never a separate input) — see the "Recurring items" bullet in the Architecture section above for the full design (`internal/handlers.applyRecurrenceCompletion`/`nextDueDate`, the "shift in place rather than clone a row" choice, the offline JS mirror in `sw.js`). The frontend's recurrence `<select>` ("Ne pas répéter"/"Tous les jours"/"Toutes les semaines"/"Tous les mois"/"Tous les ans") is in both the create-item form and the edit-item modal in `static/index.html`, applies to both list types, and a recurring item's row shows a 🔄 badge with the translated frequency (fully i18n'd, new `items.recurrence*`/`modals.editItem.recurrenceLabel` keys in `static/locales/{fr,en}.json`). Verified: Go unit tests for `internal/validate.Date`/`Recurrence` (`internal/validate/date_test.go`/`recurrence_test.go`), the completion/advance logic including the recurrence-end-date cutoff and month/year rollover (`internal/handlers/recurrence_test.go`), a DB-layer persistence test (`internal/db/items_test.go`'s `TestRecurringItemPersistence`) — `go build`/`go vet`/`gofmt`/`go test ./...` all clean — a live curl walkthrough of the full API (create with `WEEKLY`, complete twice watching `due_date` advance by 7 days each time, set a `recurrence_end_date` in the past and confirm the item then stays `done`, clear `recurrence_rule` and confirm `due_date`/`recurrence_end_date` clear with it), and three real headless-Chromium (Playwright) sessions covering the create form, the edit-modal round-trip, and — with `BrowserContext.setOffline(true)` — the offline check-off-and-advance path in `sw.js`. `EVERY_X_DAYS:<n>` is accepted by the API and both the Go and JS advance logic, but there's no UI control to set it (the dropdown only offers the four fixed cadences), so it's currently reachable only via a direct API call. **Not yet done**: no due-date display anywhere in the UI (it's tracked purely as backend bookkeeping for now, per the feature request's own scope) — a future session could surface it if that's wanted.
- **Urgent items**: an item can carry a plain `is_urgent` boolean (`INTEGER NOT NULL DEFAULT 0`, `addColumnIfMissing`-guarded the same way as `price`/`target_month`) flagging it as needing attention right away (e.g. "out of toilet paper") — unlike `is_recurring` it's not derived from anything else, and unlike `price`/`target_month` it applies to both list types. `POST`/`PUT`/`PATCH /api/v1/items` all accept it (see [docs/API.md](docs/API.md#urgent-items)); `PUT` resets it to `false` if omitted (full replace), `PATCH` leaves it untouched if omitted. The frontend adds a checkbox ("Marquer comme urgent", with an "Ex : Papier toilette épuisé" hint) to both the create-item form and the edit-item modal in `static/index.html`; an item flagged urgent shows a 🚨 "Urgent" badge on its row (`buildUrgentBadge` in `static/js/list_view.js`) and, while still unfinished, a distinctive rose border, and `renderItems`' active-item list is stably sorted so unfinished urgent items float to the top without disturbing the relative order of everything else. A third dashboard tab, "Urgent" (`static/js/urgent.js`, `#tab-urgent`/`#urgent-view`), lists every unfinished urgent item across every list (shopping and todo alike) in the current house, fanning out over `?house_id=` the same way `planning.js` does, with a one-click checkbox to mark an item done straight from the widget (a direct optimistic `PATCH`, not `list_view.js`'s undo-toast `toggleDone`, since the item isn't part of `state.currentList`) and a click-through back into that item's list (`selectList`). The three dashboard tabs' mutual-exclusion switching (`setActiveTab`) was generalized from two-way to three-way directly in `planning.js`, since it already owned that logic; `app.js`'s `refreshVisibleView` (used by the offline-sync-complete/language-switch/house-switch hooks) now also checks `isUrgentTabActive()`/`refreshUrgentIfActive()` the same way it already did for planning. `sw.js`'s `APP_SHELL` now also caches `/js/urgent.js` (cache bumped to `v12`); no offline-mirror logic needed touching since `is_urgent` is a plain field that already passes through `sw.js`'s generic `{...existing, ...body}` optimistic-edit spread, unlike recurrence's hand-kept JS port. Verified: a DB-layer persistence test (`internal/db/items_test.go`'s `TestUrgentItemPersistence`), `go build`/`go vet`/`gofmt`/`go test ./...` all clean, and a live curl walkthrough of the full API (create with `is_urgent: true`, `PATCH` to `false` and back, `PUT` full-replace behavior). **Not yet done**: no browser/Playwright verification of the frontend (checkbox, badge, sort-to-top, the urgent tab's fan-out/mark-done/click-through) — no browser automation tooling was available in the sandbox this was built in; try it in an actual browser before relying on it.
- **Price alerts / deal notifications**: a new `price_alerts` table (`id`, `item_id`, `original_price`, `found_price`, `source_url`, `status` ∈ `pending`/`accepted`/`rejected`, `created_at` — a brand-new table added directly in `schema.sql`, not `addColumnIfMissing`-guarded) records a lower price found for an item's `url` than its current `price`. Rows are created by `internal/handlers.checkItemForBetterPrice` (`internal/handlers/price_alerts.go`), which reuses the existing `internal/scraper.FetchProductInfo` (no changes needed in `internal/scraper` itself — the "comparator" is just a `<` on the two prices) and guards against duplicate alerts via `internal/db.CreatePriceAlertIfNonePending`'s `WHERE NOT EXISTS (... status = 'pending')`. Two triggers call it: a periodic background scan (`RunPriceAlertScan`, iterating `internal/db.ListItemsForPriceScan`'s not-done/has-url/has-price items, run on a `time.Ticker` wired up in `cmd/server/main.go`'s `runPriceAlertScanLoop`, interval configurable via the new `PRICE_CHECK_INTERVAL_HOURS` env var — default 24h, `<= 0` disables it) and an on-demand endpoint (`POST /api/v1/items/{id}/price-check`, bounded by the same `priceCheckTimeout` = 10s the periodic scan uses per item). `GET /api/v1/price-alerts?house_id=&status=` lists alerts (RBAC-scoped via `authorizeHouseAccess`); `PATCH /api/v1/price-alerts/{id}` with `{"status": "accepted"}` atomically applies `found_price` to the item (`price_auto: true`, inside a `sql.Tx` in `internal/db.AcceptPriceAlert` — same "must not leave inconsistent state" reasoning as `CreateHouseWithOwner`) or `{"status": "rejected"}` just dismisses it (`RejectPriceAlert`); both guard on `WHERE status = 'pending'` so an alert can only ever be actioned once, returning `409` on a repeat attempt. Full design in [docs/API.md](docs/API.md#price-alerts) and [docs/DATABASE.md](docs/DATABASE.md#price_alerts). The frontend adds a 🔔 button to the header (next to the language switcher) with a badge showing the current house's pending-alert count, opening a bottom-sheet drawer (`static/js/notifications.js`, `#notifications-modal`) listing each alert as an old-price/new-price comparison with "Appliquer le nouveau prix"/"Ignorer" buttons (optimistic, with rollback on failure, the same pattern `urgent.js`'s mark-done button uses) — unlike the planning/urgent dashboard tabs this isn't a tab, so it refreshes from its own hook points wired into `app.js` (house switch, `trakka-sync-complete`, a language switch, the page regaining visibility, and once on initial load) rather than through `refreshVisibleView`'s tab-switch logic. `sw.js`'s `APP_SHELL` now also caches `/js/notifications.js` (cache bumped to `v13`). Verified: `internal/db/price_alerts_test.go` (the duplicate-pending guard, accept applying the price + flipping status atomically, reject leaving the item untouched, the accept/reject-twice `ErrNotFound` guard, and the periodic scan's eligibility filter), `go build`/`go vet`/`gofmt`/`go test ./...` all clean. **Not yet done**: no live curl walkthrough or browser verification of the new endpoints/UI (no e-commerce site with an actual price drop was available to trigger the scraper end-to-end in the sandbox this was built in), and the frontend (bell badge, drawer, accept/reject buttons) has not been exercised in a browser — check both before relying on it, the same caveat every scraper-touching feature in this log has carried.
- **Light/dark theme**: the whole PWA (and the server-rendered `templates/login.html`) supports Light, Dark, and Auto (follows the OS `prefers-color-scheme`), chosen from a 🌙/☀️ header dropdown (`#theme-button`/`#theme-menu`, same open/close/Escape/click-outside pattern as the existing FR/EN `#lang-button`/`#lang-menu`) and persisted in `localStorage['trakka:theme']`. The mechanism is a single `data-theme="light"|"dark"` attribute on `<html>`: Tailwind's Play CDN is configured with `darkMode: ['selector', '[data-theme="dark"]']` (`static/js/tailwind-config.js`), so every hardcoded dark-only Tailwind utility class across `static/index.html`, `templates/login.html`, and the DOM-building code in `static/js/*.js` was mechanically paired with a light-mode default plus a `dark:` variant carrying the original (already-verified) dark value — e.g. `bg-slate-900` became `bg-white dark:bg-slate-900` — so dark mode's appearance is pixel-identical to before this feature; only light mode is new. `static/css/tokens.css`'s `--tk-*` custom properties back the handful of things Tailwind utilities don't reach (`color-scheme`, used so native form-control chrome — checkboxes, `<select>`, scrollbars, the `<input type="month">` picker — re-themes itself instead of staying stuck light); `static/css/base.css` adds a scoped background/border/color `transition` for a smooth manual crossfade and a `scrollbar-color`/`::selection` rule. The header lockup logo swaps between the pre-existing `trakka-lockup-light-bg.svg`/`trakka-lockup-dark-bg.svg` assets via two stacked `<img>` tags toggled with `dark:hidden`/`hidden dark:block` (pure CSS, no flash) — this asset pair already existed but only the dark variant was ever wired up before. **Anti-FOUC + a CSP gotcha worth remembering**: the theme has to be resolved and set as the `data-theme` attribute *before* any stylesheet is parsed, which normally argues for an inline `<script>` at the very top of `<head>` — but this app's CSP (`internal/handlers.SecurityHeaders`) sets `script-src 'self' https://cdn.tailwindcss.com` with no `'unsafe-inline'`, so an inline script here is silently blocked by the browser rather than executed (this was actually hit and debugged during this session — the first implementation attempt used inline `<script>` tags for both the anti-FOUC bootstrap and the `tailwind.config` assignment, and dark-mode toggling silently did nothing because Tailwind fell back to its default `media`-query strategy). The fix, and the pattern to follow for anything like this in the future: `static/js/theme-init.js` (the anti-FOUC resolver) and `static/js/tailwind-config.js` (the `darkMode` config) are both tiny external files loaded via ordinary `<script src="...">` tags — same origin, so `'self'` covers them, and a blocking `<script src>` still runs before subsequent `<link rel="stylesheet">` tags are applied, preserving the FOUC guarantee. `static/js/theme.js` (loaded later, at the bottom of `<body>`) owns the interactive parts: the header menu, persistence, updating `<meta name="theme-color" id="theme-color-meta">`, and a `matchMedia('(prefers-color-scheme: dark)')` change listener that keeps the page live-following the OS for as long as "Auto" stays selected (no reload needed). `static/sw.js`'s `APP_SHELL` now also caches `theme-init.js`/`theme.js`/`tailwind-config.js`/the light-bg logo (cache bumped to `v15`). Verified end-to-end in real headless-Chromium (Playwright) sessions covering: the login/register page respecting OS light and OS dark with zero JS (confirms the `@media (prefers-color-scheme: dark)` fallback in `tokens.css` independently of `theme.js`); the full dashboard, item list (including the urgent-item rose border/badge), edit-item modal, planning tab, urgent tab, admin-settings/members/new-house modals, and the error banner all in explicit Light; explicit Dark; reload-persistence of an explicit choice; and Auto live-following a simulated OS preference change without a reload — zero console errors in any of it. **Not yet done**: `templates/login.html` has no header/toggle of its own (it's unauthenticated and has no header at all) — it only benefits from an explicit choice because it shares `localStorage` with the app on the same origin; a user whose very first visit is the login page still only gets OS-based Auto, which is fine since that's this page's existing precedent (see the pre-existing "login.html is still French-only" gap below).
- **Houses**: a grouping/sharing concept — every list belongs to exactly one house (`lists.house_id`, added via the `addColumnIfMissing` guard since `lists` already had a shipped shape), and every house has one or more members via `house_members`. `ensureDefaultHouse()`'s pre-auth "Maison Principale" seed (`internal/db/db.go`) still runs on a fresh database but is now a harmless, permanently orphaned row (no members, so RBAC hides it from everyone) — see docs/DATABASE.md's Houses section for why it wasn't worth special-casing away.
- **SQLite database** (`modernc.org/sqlite`, pure Go) with idempotent schema, `ON DELETE CASCADE` chains (house → list → item; house → house_members; user → sessions/house_members), single-connection pool — see [docs/DATABASE.md](docs/DATABASE.md).
- **Complete, working PWA frontend**: `static/index.html` (+ logout button, members-management modal), `static/js/app.js` (dashboard, house selector, list management, auth-gate via `apiRequest()`'s 401 handling, house-member invite/remove UI), `static/js/list_view.js` ("Google Tasks"-style list detail view), `static/js/db.js` (shared IndexedDB layer, `DB_VERSION` 2 — unchanged by this feature, since the HttpOnly session cookie has nothing to mirror), `static/sw.js` (service worker: app-shell caching, offline write queue, temp-id reconciliation, a `401`-leaves-entry-queued fix so an expired session can't silently drop an offline edit, `/auth/...` bypass; houses are read-mirrored but require connectivity to mutate — see docs/PWA.md), `static/manifest.json`, cross-platform icons. Tailwind via Play CDN, minimal custom CSS. Verified end-to-end with the same headless-Chromium (Playwright) approach used for the houses feature.
- **Offline/PWA support** fully designed and documented in [docs/PWA.md](docs/PWA.md), including the historical `remapTempId`/cascade-delete ordering bug (houses feature) and the newer session-expiry-vs-queue fix (this feature), both found by scripted testing.
- **Deployment**: multi-stage `Dockerfile` (`golang:1.27.0-alpine` builder → `gcr.io/distroless/static-debian12` runtime, fixed non-root UID/GID `10001:10001`, `CGO_ENABLED=0`, `COPY`s `templates/`), `compose.yml` (`trakka` service + optional `radicale` service behind the `calendar` profile), in-process healthcheck. New env vars (`BASE_URL`, `OIDC_*`, `SESSION_*`, `TEMPLATES_DIR`) documented in [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md). The Docker image build itself was **not** verified in the session that added auth, nor in the later session that switched the runtime base to distroless and added `read_only`/`cap_drop` hardening to `compose.yml` — no Docker daemon was available in either sandbox. Both changes are low-risk (the distroless switch mirrors the well-known "prepare passwd/group/data-dir in the builder stage, `COPY --from=build` them into a shell-less runtime" pattern; nothing in the app writes anywhere but `/data`), but build and run it at least once before deploying — especially the `read_only: true` root filesystem, which is the part most likely to surface a startup issue than the base-image swap itself.
- **Documentation**: `README.md` + `docs/API.md`, `docs/DATABASE.md`, `docs/DEPLOYMENT.md`, `docs/DEVELOPMENT.md`, `docs/PWA.md`, all current and cross-linked with this file. The `docs/DEVELOPMENT.md` gap that used to be tracked here (missing `list_view.js` in the project layout) is fixed.
- **Custom categories ("Spaces")** — backend only: a new `custom_categories` table (`id`, `user_id`, `name`, `icon`, `color`, `position`, `created_at`) plus `lists.custom_category_id` (nullable, `addColumnIfMissing`-guarded, `REFERENCES custom_categories(id) ON DELETE SET NULL`) — see the dedicated Architecture bullet above for the full design (per-user ownership scoping, the `ON DELETE SET NULL` reasoning, and why the `addColumnIfMissing` call had to be sequenced after `migrateListsTypeCheck()` in `internal/db/db.go`). `models.CustomCategory` (`internal/models/models.go`) and CRUD in `internal/db/custom_categories.go`; `internal/handlers/categories.go` exposes `GET`/`POST /api/v1/custom-categories` and `PUT`/`DELETE /api/v1/custom-categories/{id}` (no partial `PATCH` — matches houses' own CRUD shape, which also has no `PATCH`); `internal/handlers/lists.go`'s `handleListsCreate`/`handleListsUpdate` accept an optional `custom_category_id` (validated by the new `validateCustomCategoryOwnership` against the *caller's* own categories) and `internal/db.GetList`/`ListListsForUser` embed the attached category via a `LEFT JOIN` (`custom_category` in the JSON response). A new `internal/validate.Color` validates an optional `#RGB`/`#RRGGBB` hex string, following the same "empty is valid, non-empty must match" shape as `Date`/`Month`/`Recurrence`. Documented in [docs/API.md](docs/API.md#custom-categories) and [docs/DATABASE.md](docs/DATABASE.md#custom_categories). Verified: `internal/db/custom_categories_test.go` (CRUD, cross-user ownership scoping returning `ErrNotFound` rather than leaking existence, list association/embedding via both `GetList` and `ListListsForUser`, dissociation, and `ON DELETE SET NULL` when a category is deleted out from under an attached list), `go build`/`go vet`/`gofmt`/`go test ./...` all clean.
- **Custom categories frontend**: a fourth dashboard tab, "Espaces" (`static/js/spaces.js`, `#tab-spaces`/`#spaces-view`), showing a small violet dot on the tab button (`#tab-spaces-badge`) whenever the user has at least one category, updated by `loadCustomCategories()` at startup and after every category create/edit/delete. The tab renders each category as a collapsible `<details>` section (the same chevron/`group`/`group-open:rotate-90` idiom as `#done-section`) with an edit (pencil) and delete (trash) icon-button in its `<summary>`, holding a grid of every list attached to it (`buildListCard`, reused directly from `app.js`, with a per-type badge function — `urlBadges`/`progressBadge`/`noBadges` — chosen per list since a single space can mix `type`s) fetched via the same `?house_id=` + per-list `GET /lists/{id}` fan-out `planning.js`/`urgent.js` already use, filtered to lists carrying a `custom_category_id`. Zero categories shows an empty state with a "+ Créer une catégorie personnalisée" button. The three-way tab switcher in `planning.js` (`setActiveTab`/`activeTab`) was generalized to four; `app.js`'s `refreshVisibleView` gained an `isSpacesTabActive()`/`refreshSpacesIfActive()` branch the same way it already had one each for planning/urgent. A shared create/edit modal (`#category-modal`, `#category-form`) collects a name, a freeform emoji `icon` (a text input plus eight one-click emoji preset buttons), and a `color` (a native `<input type="color">`, which already only ever emits a `#rrggbb` string — satisfying `internal/validate.Color` for free — plus six preset swatch buttons); category deletion goes through the same 5s undo-toast pattern as `removeList` in `app.js` (`TrakkaUndo.schedule`), relying on the backend's `ON DELETE SET NULL` so a deleted category's lists are never themselves touched. The "new list" modal (`static/index.html`'s `#create-list-form`, submit handler in `app.js`) gained a `#list-category` `<select>` (populated by `spaces.js`'s `populateCategorySelect`, called every time the modal opens so it can't go stale) sending `custom_category_id` on `POST /api/v1/lists`; picking its "+ Créer un nouvel espace" sentinel option (`CREATE_CATEGORY_OPTION_VALUE`, mirroring `CREATE_HOUSE_OPTION_VALUE`'s pattern for the house selector) opens the category modal *on top of* the still-open list modal, and its `onCreated` callback selects the freshly created category back in the list modal's picker once saved — the only two places in the app where one modal can currently open on top of another, which needed two small guards: `closeCategoryModal` only releases the shared `overflow-hidden` body class if the list modal isn't still open, and the list modal's own Escape handler skips closing while the category modal is the one on top of it. A dedicated "edit list" modal (reassigning name/type/category, and now icon) was added in a later session — see the "List editing" bullet below; the previous sentence in this bullet claiming no such modal exists is now out of date and kept only for history. `sw.js`'s `APP_SHELL` now also caches `/js/spaces.js` (cache bumped to `v20`); new `spaces.*`/`modals.newCategory.*`/`modals.newList.category*`/`undo.categoryDeleted` keys added to both `static/locales/{fr,en}.json`. **Not yet done**: no live browser verification — no browser automation tooling was available in the sandbox this was built in (same caveat as the urgent-items and price-alerts frontends above) — only `go build`/`go vet`/`gofmt` (unaffected, since this is a frontend-only change) were checked. Before relying on it: exercise the Espaces tab's empty state, create/edit/delete a category (including the undo toast), the nested "create space from the list modal" flow end to end, and the mixed-type list grouping in a real browser.
- **List editing**: a list's `name`, `type`, `custom_category_id`, and a new freeform `icon` (typically an emoji, e.g. `🛒`/`🖥️`/`📦`) can all be changed after creation, not just at creation time. `lists.icon` (`TEXT NOT NULL DEFAULT ''`, added the same way as `custom_category_id` — at the time, `addColumnIfMissing`-guarded and sequenced after `migrateListsTypeCheck()` for the "the rebuild would silently drop it" reason; both are now migrations 8 and 6 respectively in the versioned engine described in the Architecture section, where sequencing is enforced by migration order instead) backs `models.List.Icon`; `internal/handlers.handleListsCreate`/`handleListsUpdate` both accept it (trimmed, no further validation, same convention as `custom_categories.icon`), and `PUT /api/v1/lists/{id}` — already a full replace — now also resets `icon` to `""` if omitted, matching how `type`/`custom_category_id` already behave there. The frontend turns the existing "new list" modal (`#new-list-modal`) into a shared create/edit modal exactly the way `spaces.js`'s category modal already does (`editingList` state var in `app.js`'s `openListModal(list)`, `null` meaning create mode; swaps the modal's title/submit button text via new `modals.newList.title{Create,Edit}`/`submit{Create,Edit}` i18n keys), adding an icon text input plus five one-click emoji presets. A ✏️ edit button now sits next to every list card's 🗑️ delete button on the dashboard grids *and* the Espaces tab (both render through the same shared `buildListCard` in `app.js`), and next to the list-detail header (`#edit-list-button` in `list_view.js`, opening the modal prefilled from `state.currentList`); saving an edit calls `refreshCurrentList()` afterward so the open detail view picks up the change immediately (a no-op if a different list, or no list, is open). A list's own `icon` is shown before its name — falling back to a fixed per-`type` default (`listIcon()` in `app.js`, reusing `LIST_TYPE_BADGE_META`'s icons) when unset — on every list card (dashboard grids and Espaces cards alike, since both share `buildListCard`) and in the list-detail header. `icon` is a plain field already covered by `sw.js`'s generic `{...existing, ...body}` optimistic-edit spread for `PUT /api/v1/lists/{id}` (lists are already offline-queueable via `temp-list-*` ids, unlike houses), so no offline-mirror logic needed touching. `sw.js`'s `APP_SHELL` cache bumped to `v21` (content-only change, no new cached paths). Verified: `go build`/`go vet`/`gofmt`/`go test ./...` clean, plus a live curl walkthrough (register, create a list with `icon`, `GET` reflects it, `PUT` changes name/type/icon, a `PUT` omitting `icon` clears it back to `""`). **Not yet done**: no live browser verification of the frontend (the edit button on cards/header, the modal's prefill and title/submit swap, the icon presets, the icon showing up on cards and the detail header) — no browser automation tooling was available in the sandbox this was built in, same caveat as every other frontend-only feature in this log.
- **Offline UX pass on the Budget/Urgent/Espaces tabs**: previously, `planning.js`'s `loadPlanningView`/`urgent.js`'s `loadUrgentView`/`spaces.js`'s `loadSpacesView`/`loadCustomCategories` had no IndexedDB fallback at all — a failed `apiRequest()` (offline, or a transient server error) just emptied `planningEntries`/`urgentEntries`/`spacesLists`/`customCategories` and popped the blocking error banner, so those three tabs went completely blank the moment the network dropped, unlike the dashboard (`loadDashboard`) which already fell back to its `cachedDashboardLists()` mirror. Fixed by giving all three the same fallback: on failure, they now re-derive their entries from `cachedDashboardLists(state.currentHouseId)` (the existing helper in `app.js`, reused as-is rather than duplicated) via new shared extraction helpers (`applyPlanningEntries` in `planning.js`, `extractUrgentEntries` in `urgent.js`) so the live-network and cache-fallback paths can't drift apart. Custom categories themselves had no IndexedDB mirror at all (only the lists/items/houses stores existed) — added a `custom_categories` object store to `static/js/db.js` (`DB_VERSION` bumped `2` → `3`, `getCustomCategories`/`putCustomCategories`/`putCustomCategory`/`deleteCustomCategory`) and wired `static/sw.js` to mirror `GET/POST/PUT/DELETE /api/v1/custom-categories` into it the same way houses/lists/items already are, including an `offlineReadFallback()` branch that now serves a real cached `200` for `GET /api/v1/custom-categories` while offline instead of falling through to a generic `503` (`sw.js`'s `SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v22` accordingly). Separately, the blocking error banner (`showError()`) was firing on every plain connectivity failure during ordinary read-driven navigation (opening a tab, switching houses, a background refresh) even when a cache fallback had *already* repainted the view with something to show — redundant with the header's discreet "Hors-ligne" badge and, per explicit user feedback, intrusive. Fixed at the source: `apiRequest()` (`app.js`) now tags the `Error` it throws when `fetch()` itself fails (no route to the server) with `err.isNetworkError = true`, distinct from the untagged `Error` thrown for a non-2xx HTTP response; a new `isNetworkError(err)` helper is checked before every `showError()` call in every GET-driven loader (`loadHouses`/`loadDashboard`/`loadMembers` in `app.js`; `selectList`/`refreshCurrentList` in `list_view.js`; the three tabs' loaders above) so a plain offline navigation stays silent while a genuine server-side error (a `5xx`, a bug) still raises the banner. List/item metadata needed for card rendering while offline (`icon`, `type`, `custom_category_id`, badge-driving fields) required no changes — `sw.js`'s existing `withoutItems()` spread already preserves every list field verbatim into the mirror, and `buildListCard`/`listIcon`/`typeBadge` in `app.js` already read from whatever list object they're given (live or cached) identically. Full design rationale documented in [docs/PWA.md](docs/PWA.md) (new "Custom categories are read-mirrored but not offline-writable" section, mirroring the pre-existing houses one, plus a new paragraph under "How offline reads and writes work" explaining the `isNetworkError` convention). **Not yet done**: no live browser verification — no browser automation tooling was available in the sandbox this was built in (same caveat as most other frontend work in this log). Before relying on it: go offline mid-session (DevTools' Network throttling → Offline, or airplane mode) with each of the three tabs already visited once online, confirm they keep showing their last-synced data with no error banner, and confirm the banner still appears for an actual server error (e.g. temporarily stop the backend and hit a `5xx`/connection-refused case if that's distinguishable from a network error in your test setup). Also note: creating/editing/deleting a *custom category itself* while offline is still not a supported flow (only reading the last-synced set is) — see the new PWA.md section for why.
- **Offline reload follow-up: skip the doomed fetch instead of just catching it**: the offline UX pass above already made every GET-driven loader fall back to IndexedDB on failure, but it still *attempted* `fetch()` first every time, even when the browser already knew there was no connectivity — without a controlling service worker (or on the very first request after a fresh `F5` before one takes over), a doomed `fetch()` can take several seconds to time out rather than reject instantly, so a reload while offline could sit stalled before its cache fallback ever kicked in. Fixed centrally in `apiRequest()` (`app.js`): when called for a `GET` and `navigator.onLine` is already `false`, it now throws the same `isNetworkError`-tagged `Error` immediately, without ever calling `fetch()` — this automatically speeds up *every* GET-driven caller (`loadHouses`/`loadDashboard`/`loadMembers` in `app.js`; `selectList`/`refreshCurrentList` in `list_view.js`; `loadPlanningView`/`loadUrgentView`/`loadSpacesView`/`loadCustomCategories` in `planning.js`/`urgent.js`/`spaces.js`) with no per-file changes needed, since they already funnel through this one function. Deliberately scoped to `GET` only: a write (`POST`/`PUT`/`PATCH`/`DELETE`) still always calls `fetch()` even while offline, because that's what lets the service worker's `handleApiWrite` intercept it and queue it in IndexedDB for later replay (`sw.js`'s `queueOfflineWrite`) — short-circuiting before `fetch()` for a write would silently drop it instead of queuing it. `navigator.onLine` reporting `true` on a connection that can't actually reach the server (a captive portal, a downed backend) is unaffected and still runs the normal `fetch()`-then-catch path exactly as before. Separately, `hydrateFromCache()` (`app.js`, the cache-only paint `init()` runs before any network call) previously only warmed houses/lists/items — it now also reads the `custom_categories` IndexedDB store directly and calls `updateSpacesTabBadge()` (both defined in `spaces.js`), so the "Espaces" tab's highlight dot and the "new list" modal's category picker are correct immediately on boot rather than only after `loadCustomCategories()`'s own network-first call fails over further down in `init()`. Also hardened `db.js`'s `open()`: it memoizes its connection promise (`dbPromise`) so every store helper shares one `indexedDB.open()` call, but previously never cleared that memo on `onerror` — meaning one transient failure (a browser hiccup, a version-upgrade conflict with another open tab) would have permanently wedged the entire IndexedDB mirror, and every offline fallback built on it, for the rest of the page's lifetime; it now resets `dbPromise = null` before rejecting so the next call retries with a fresh request. **Not yet done**: same caveat as the pass above — no browser automation tooling was available to actually click through an offline `F5` reload landing on each tab; verify with DevTools' Network → Offline before relying on it.
- Verified on 2026-08-28: `go build`, `go vet ./...`, `gofmt -l .`, and `go test ./...` are clean on the current state of the repo (this session's changes were frontend-only — `static/js/{app,db,list_view,planning,urgent,spaces}.js`, `static/sw.js` — so none of these commands touch the changed files, but re-running them after a frontend-only change is still cheap confirmation nothing else regressed).
- **Granular sharing (Spaces & Lists)**: extends the existing House-only sharing model with two new tables, `space_shares`/`list_shares`, so a Space or an individual List can be shared directly with one other user (by email, `read` or `write`) without adding them to the whole parent House — see the dedicated "Granular sharing" bullet in the Architecture section above for the full design (`internal/db.AccessLevelForList`'s three-source combined verdict, `internal/handlers.authorizeListAccess`/`authorizeItemAccess`'s new `requireWrite` parameter, why List-sharing management stays House-membership-only while Space-sharing stays owner-only, `ListSharedListsForUser` backing the new `?shared_with_me=true` query and the "Partagé avec moi" tab). New backend files: `internal/db/shares.go`, `internal/handlers/shares.go`; new frontend file: `static/js/shares.js` (the share modal + the fifth dashboard tab, `#tab-shared`/`#shared-view`); a 👥 share button was added to every list card (`buildListCard` in `app.js`), every space section (`spaces.js`), and the list-detail header (`#share-list-button` in `list_view.js`). `sw.js` blocks POST/DELETE on `.../share` while offline (same "needs an immediate round-trip, queuing it would surface a confusing later failure" reasoning as houses/admin-settings) and excludes `?shared_with_me=true` results from the general lists IndexedDB mirror (`SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v23`). Documented in [docs/API.md](docs/API.md#sharing) and [docs/DATABASE.md](docs/DATABASE.md#space_shares-and-list_shares). Verified: `internal/db/shares_test.go` (share CRUD/upsert for both tables, `AccessLevelForList` exercising all three sources including the "higher of two shares wins" case, `ListSharedListsForUser`'s house-membership exclusion) — `go build`/`go vet`/`gofmt`/`go test ./...` all clean — plus a full live curl walkthrough of the backend end-to-end: sharing a list (read) and a space (write) with a second registered user, confirming `?shared_with_me=true` lists both with the right `access_source`/`access_permission`, confirming read-only enforcement (renaming/adding an item to the read-shared list both correctly `403`), confirming write-via-space access (adding an item to the space-shared list succeeds), the roster endpoint being House-membership-gated (`403` for the non-member recipient), self-share/unknown-email/non-owner-sharing-a-category-they-don't-own all rejected with the right status codes (`400`/`404`/`404`, the last never leaking the category's existence), revoke working and being non-idempotent (`404` on a repeat call), and a shared list correctly dropping out of `?shared_with_me=true` once the recipient becomes an actual House member (already visible the ordinary way). **Not yet done**: no browser verification of the frontend (the 👥 buttons, the share modal's roster/grant/revoke flow, the "Partagé avec moi" tab's card rendering and empty state) — no browser automation tooling was available in the sandbox this was built in, same caveat as every other frontend-only feature in this log.
- **Versioned SQLite migration engine, pre-migration backups, service worker update banner, IndexedDB upgrade hardening**: replaces the single idempotent `internal/db/schema.sql` + `addColumnIfMissing()`/`migrateListsTypeCheck()` mechanism with a real versioned migration engine — see the dedicated Architecture bullet above and [docs/DATABASE.md](docs/DATABASE.md#evolving-the-schema) for the full design. `internal/db/migrations/0001_init.sql` through `0009_granular_sharing.sql` (migration 6, widening `lists.type`'s `CHECK` constraint, is Go code in `internal/db/migrate_list_types.go` instead, since it needs `PRAGMA foreign_keys = OFF` set outside any transaction) reconstruct the schema's *actual* history one step at a time, reverse-engineered from `git log` on `internal/db/schema.sql`/`db.go` rather than guessed, so a fresh install replays the same sequence of changes a long-lived deployment actually went through. `internal/db/migrate.go`'s `hasExistingSchema` adopts a pre-existing database (tables present, `PRAGMA user_version` still `0`) directly at the latest version without re-running any migration SQL, which the pre-migration-engine code had already applied unconditionally on every prior startup — the load-bearing invariant this relies on is that `Open()` always refused to start on any failure, so no database that ever started successfully can be missing part of the schema. `backupBeforeMigration` snapshots an already-versioned, non-empty database via `VACUUM INTO` (confirmed to accept a bound parameter through `modernc.org/sqlite`, kept as an ordinary parameterized statement) to `<DB_PATH's directory>/backups/` before applying any pending migration — `/data/backups/`, for the default `DB_PATH`, already writable under `compose.yml`'s `read_only: true` root filesystem since it's inside the `/data` named volume; nothing prunes old backups automatically yet. `internal/db.Open` now takes a `*slog.Logger` (only used to record which migrations ran — the common "already up to date" case logs nothing), so every call site (`cmd/server/main.go`, and the `openTestDB` helpers in `internal/db`'s and `internal/settings`'s test files) was updated to pass one. On the frontend: `static/js/app.js`'s `registerServiceWorker()` now calls `watchForServiceWorkerUpdate(registration)`, which listens for a new service worker reaching `'installed'` while `navigator.serviceWorker.controller` is already set (the standard "this is a genuine update, not the first-ever install" signal) and shows a new discreet `#update-banner` ("Une nouvelle version est disponible." / "Mettre à jour", new `update.*` i18n keys) rather than reloading automatically — reload only happens if the user clicks the button, so nothing forces a loss of in-progress form input. This works specifically because `sw.js`'s existing `self.skipWaiting()` (install) and `self.clients.claim()` (activate) calls — already present before this session, not new — mean the new worker actually takes over almost immediately once installed, so a click on the banner's button really does pick up the new assets rather than reloading into the same old ones; `sw.js` gained a couple of comments tying this together but no behavior changes. `static/js/db.js`'s `open()` gained `request.onblocked` (logs a warning) and, on every successful connection, `db.onversionchange` (proactively closes that connection and clears the memoized `dbPromise`) — the standard multi-tab IndexedDB upgrade pattern, so a `DB_VERSION` bump in a future deployment can't leave an older tab's still-open connection blocking a newer tab's `onupgradeneeded` from ever running; `onupgradeneeded` itself was already purely additive (`objectStoreNames.contains` guards before every `createObjectStore`, never dropping a store) from earlier work, unchanged here beyond a clarifying comment. Verified: three new Go tests in `internal/db/migrate_test.go` (`TestMigrateFreshDatabase` — a brand new database ends at the latest `user_version` with a schema whose `lists.type` CHECK is already widened; `TestMigrateAdoptsPreExistingDatabase` — a simulated pre-migration-engine database is adopted without data loss and without attempting to back it up; `TestBackupBeforeMigrationCreatesSnapshot` — the `VACUUM INTO` snapshot actually contains the pre-migration data; plus `TestHasExistingSchema`/`TestLoadMigrationsSequential`), `go build`/`go vet ./...`/`gofmt -l .`/`go test ./...` all clean. **Not yet done**: no browser verification of the update banner (no browser automation tooling was available in the sandbox this was built in, same caveat as most frontend work in this log) — check that a real deployment (bump `SHELL_CACHE`/`RUNTIME_CACHE` in `sw.js`, redeploy, reload an already-open tab) actually surfaces the banner and that clicking it reloads onto the new assets. The `hasExistingSchema`-adoption path was only exercised against a synthetic minimal seed (a `houses`/`lists` pair) in the test above, not against a real pre-existing `/data/trakka.db` carrying the full current schema and real data — do that once before relying on it in a real upgrade. No backup-pruning/retention policy exists yet for `/data/backups/`.
- **Hardened GitHub Actions CI/CD pipeline**: `.github/workflows/ci.yml` replaces the previous ad-hoc workflow with a four-job pipeline (`lint` → `test` → `security-scan` → `build-scan-push`) built around two supply-chain rules — every action pinned to a full commit SHA (never a floating tag) and `permissions: read-all` at the workflow level with each job granting back only what it actually needs — see the new "CI/CD & Sécurité" section above for the complete design (job responsibilities, the permissions table, the SHA-pinning rule and how to apply it to a new action, local-equivalent commands). Turning `golangci-lint` and `gosec` into real CI gates for the first time surfaced pre-existing findings that had to be triaged rather than ignored: a new `.golangci.yml` excludes `errcheck`'s check on the `Close`/`Rollback` deferred-cleanup pattern already used throughout `internal/db` (13 pre-existing hits, all the same harmless idiom — see the new section for the exact exclusion list), `internal/db/migrate.go`'s pre-migration backup directory now uses `0o750` instead of `0o755` (gosec G301 — the directory holds full SQLite snapshots, so it shouldn't be world-readable, and this was a real, if minor, gap worth fixing outright), and five `#nosec` comments were added at genuine gosec false positives rather than being silenced globally: two `G101` ("potential hardcoded credentials") on a settings-table key name and a map of UI error-message strings, and three `G124` (missing/insecure cookie attributes) on `http.Cookie` literals whose `Secure` field is already correctly wired to `SESSION_COOKIE_SECURE` via a variable (`s.CookieSecure`/`app.Auth.CookieSecure`) rather than a literal `true`, which is all gosec's G124 check actually looks for. One of the four originally-flagged cookie sites — `internal/handlers/auth.go`'s OIDC callback clearing its single-use flow cookie — turned out to be a genuine gap rather than a false positive (it set no `HttpOnly`/`Secure`/`SameSite` at all) and now mirrors the attributes of the cookie it's clearing. `go build`/`go vet`/`gofmt -l .`/`go test ./...`/`golangci-lint run ./...`/`govulncheck ./...`/`gosec ./...` were all re-run locally after these fixes and are clean. **Not yet done**: the workflow has never actually run on GitHub's runners — see the dedicated bullet under "What's left" below.

- **Item quantity is now correctly multiplied into every price total**: `items.quantity` (and `PATCH /api/v1/items/{id}`'s existing `{"quantity": N}` support, validated positive server-side — already present, no backend change was needed) previously had no effect on any displayed total — `updateFinanceSummary` in `static/js/list_view.js` summed raw `item.price` across a list's items, and `planning.js`'s budget-forecast view did the same, both silently ignoring `quantity`, so a "×3" item only ever counted once. Fixed by introducing `lineTotal(item)` in `list_view.js` (`price * (quantity > 0 ? quantity : 1)`, since `item.price` is stored and entered as a *per-unit* price, never a line total) and routing every total through it: `updateFinanceSummary`'s Total estimé/Déjà dépensé/Reste à dépenser trio, `planning.js`'s per-month and grand budget totals (both the one-off `target_month` entries and the recurring-item occurrence projections, which now multiply `price * quantity * occurrenceCount`), and a new estimated-total badge on every purchase-type list card (`static/js/app.js`'s `urlBadges`, shown on the dashboard's "Achats & Sourcing" grid and, since it's shared, the "Espaces" tab too) that sums `price * quantity` across a list's priced items. An item's row in the list detail view now shows its line subtotal as the primary price figure, with a small secondary `(unit price/u)` label once `quantity > 1` so the per-unit price used to compute it stays visible. Quantity itself is now directly editable from the list detail view — a `[-]`/number-input/`[+]` stepper (`buildQuantityStepper`/`changeQuantity` in `list_view.js`, replacing the old read-only "title × N" title suffix for any list type that shows quantity at all) fires an optimistic `PATCH /api/v1/items/{id}` with `{"quantity": N}` on every change, immediately re-rendering the row's/list's totals from local state; a `pendingQuantityUpdates` map (mirroring the existing `pendingToggles` pattern `toggleDone` already uses) tracks an in-flight request's last-known-good value plus a `Symbol` request id per item, so a slower, now-stale response from an earlier click can never clobber a value the user has since moved past with further clicks, and a failed request rolls back to that last-known-good value with the existing error banner. No `sw.js`/IndexedDB changes were needed for either the read or write side: `quantity` is a plain field already covered by the service worker's generic `{...existing, ...body}` optimistic-edit spread (offline) and its generic `mirrorWriteResult` (online) — the same generic paths `is_urgent`/`icon` already ride on — so the IndexedDB mirror and every cache-fallback view already stay in sync automatically. Verified: `go build`/`go vet ./...`/`gofmt -l .`/`go test ./...` all clean (no Go files were touched — the bug and its fix were entirely in the frontend's price-summing logic) and `node --check` on all three edited JS files. **Not yet done**: no live browser verification — no browser automation tooling was available in the sandbox this was built in, same caveat as most other frontend work in this log. Before relying on it: open a shopping list, set an item's price and bump its quantity via the new stepper (buttons and typing directly into the field), confirm the row's subtotal, the finance summary bar, the dashboard/Espaces card badge, and the Budget/Prévisions tab's totals all update consistently and immediately; also click the stepper rapidly (or throttle the network) to sanity-check the request-coalescing rollback behavior described above.

- **Mobile/PWA login loop fix**: local-password and OIDC login both appeared to succeed (`POST /auth/login` → `302`) but the very next call, `GET /api/v1/me`, immediately came back `401` and bounced the user back to `/auth/login` — a real report from mobile browsers, most consistently reproducible in iOS Safari's "Add to Home Screen" standalone/PWA mode. Root cause: the session cookie (`internal/auth.Service.SetSessionCookie`) was `SameSite=Strict`, and mobile WebKit has a well-documented quirk where a `Strict` cookie set on the response of a same-site redirect (`finishLogin`'s `302` to `/`) isn't reliably attached to the page's very next request — this is exactly the reasoning the pre-existing OIDC flow cookie (`oidcFlowCookieName` in `internal/handlers/auth.go`) already used to justify `SameSite=Lax` for itself, just not yet applied to the session cookie. Fixed by changing the session cookie to `SameSite=Lax` in both `SetSessionCookie` and `ClearSessionCookie` (`internal/auth/service.go`) — see the updated CSRF bullet in the Security rules section above for why this doesn't weaken CSRF protection (every mutating request is `fetch`/XHR, never carried cross-site under Lax either; every `GET` route is a pure read). Also added an explicit `Max-Age` alongside the existing `Expires` on the session cookie (older WebKit has been inconsistent about `Expires`-only `Set-Cookie` headers), and made `credentials: 'same-origin'` explicit on both `apiRequest()`'s `fetch()` in `static/js/app.js` and the offline-queue replay `fetch()` in `static/sw.js`'s `flushQueue()` — `'same-origin'` has been the spec default since 2017, but embedded/WebKit mobile browsers have a history of being unreliable about implicit fetch defaults, so this is now explicit as defense in depth rather than relied upon implicitly. Verified: `go build`/`go vet ./...`/`gofmt -l .` clean, `node --check` on both edited JS files. **Not yet done**: no verification on an actual mobile device or iOS Safari standalone/PWA session — no mobile browser or device was available in the sandbox this was built in. Before considering this closed: install the PWA to an iOS home screen (or at minimum test in mobile Safari/Chrome dev tools device emulation), log in, and confirm `/api/v1/me` succeeds on the first call after the post-login redirect, both for local-password login and (if a provider is available) OIDC.

- **List detail view redesigned Mobile-First (Todoist/Notion-style)**: `static/js/list_view.js`, `static/index.html`, and `static/css/base.css` reworked the item-list UI end to end, replacing the old always-expanded, all-fields-visible create-item form and single dense desktop-only row with a layout that's the same shape at every breakpoint rather than reflowing via CSS `order` on a narrow screen. Four pieces:
  - **Quick-add bar with a collapsible advanced panel**: `#create-item-form` is now just a name `<input>` plus a `[⚙️]` toggle and a `[+]` submit button by default; `[⚙️]` (`#quick-add-toggle`) expands `#quick-add-advanced` (URL/quantity/price/target month/recurrence/urgent — exactly the fields `data-item-field` already gated per list type) in place, collapsing again automatically after a successful add (`setQuickAddAdvancedExpanded`, called from the form's submit handler). The toggle itself hides outright for a `custom` list, which has none of those fields to reveal (`hasAdvancedFields`, shared between `applyListTypeVisibility` and the FAB flow below so the two can't drift apart). A `[+]` **FAB** (`#add-item-fab`, `md:hidden` — mobile only, and only ever visible at all while a list is open since it's a child of `#items-section`) opens `#create-item-form` as a bottom sheet (`#add-item-sheet`) instead of a second, duplicated form: `openAddItemSheet`/`closeAddItemSheet` simply relocate the one real `<form>` (and its already-attached listeners) between its inline slot (`#create-item-form-anchor`) and the sheet's body via plain `appendChild` on an already-attached node — moves it, doesn't clone it — with the advanced panel force-expanded while it's the dedicated add flow. An add made from the FAB sheet closes the sheet afterward (the "add one item" mobile pattern, matching this file's own "Google Tasks"-style framing); an add from the always-visible inline bar just collapses the panel back for the next entry.
  - **Item cards restructured**: `buildItemRow` now builds a top `.item-card__row` (checkbox/line-marker + optional thumbnail + bold title + inline 🔗 icon-link when the item has a safe URL, replacing the old standalone "lien ↗" pill badge — folding it into the title line is what keeps the row's one-glance hierarchy simple) with the price prominently trailing on the right (`buildPriceBlock`: the line subtotal, the auto-detected-price sparkle badge, and, once quantity > 1, the small `(unit price/u)` label — or a pending-price badge when there's a URL but no price yet), plus edit/delete (`.item-card__actions`) or a `[⋮]` kebab (`.item-card__kebab`) at the end — both are always built, and which one is actually visible is a pure `hidden md:flex`/`flex md:hidden` Tailwind concern (Tailwind's CDN default `md` breakpoint is 768px, matching every other breakpoint already hand-rolled in `base.css`). A second line, `.item-card__secondary` (only appended when it has anything in it), carries the already-existing `[-]`/N/`[+]` quantity stepper (no native `<input type=number>` spin arrows — it was already a custom styled control before this redesign) plus the month/recurrence/urgent badges, indented to align under the title.
  - **Actions simplified via a bottom sheet on mobile**: tapping an item's title, or its `[⋮]` kebab button, opens `#item-actions-sheet` (populated per-item by `openItemActionsSheet`) with ✏️ Modifier/🔗 Ouvrir le lien (hidden when the item has no safe URL)/🚨 Basculer en urgent (label flips to "Retirer l'urgence" when already urgent)/🗑️ Supprimer (in rose). "Basculer en urgent" is a new `toggleUrgent` optimistic PATCH, coalesced against concurrent toggles the same way `changeQuantity` already coalesces quantity clicks (a `pendingUrgentUpdates` map keyed by item, tracking the last-confirmed value and an in-flight request id so a slower stale response can never clobber a value the user has since moved past). The pencil/trash buttons stay visible and directly clickable on wider screens (`md:flex`) — the sheet exists specifically because two more 44px tap targets on an already-busy row crowd the price/title on a narrow phone.
  - **Collapsible budget summary**: `#finance-summary` is now a plain `<details>`/`<summary>` (the same collapsible idiom `#done-section` already used) rather than a permanently-expanded `<div>`, showing a compact always-visible total next to the chevron even while collapsed (`#finance-total-compact`, updated alongside `#finance-total` in `updateFinanceSummary`) so collapsing it to free up screen space on a small phone doesn't hide the one number most worth keeping at a glance. The open/closed choice persists across visits via `localStorage['trakka:financeSummaryCollapsed']`, the same per-browser-only persistence convention `HOUSE_STORAGE_KEY`/theme/language already use.

  New i18n keys (`items.financeSummaryLabel`/`advancedToggleAriaLabel`/`addFabAriaLabel`/`addSheetTitle`/`openLinkAriaLabel`/`moreActionsAriaLabel`, `modals.itemActions.*`) added to both `static/locales/{fr,en}.json`; `static/sw.js`'s `SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v24` (content-only change to already-cached `index.html`/`list_view.js`/`base.css`, no new cached paths). Verified: `go build`/`go vet`/`gofmt` clean (no Go files touched), `node --check` on the edited JS, a Python `html.parser` pass confirming `index.html`'s tags still balance, and — unlike most other frontend-only work in this log — **a real headless-Chromium (Playwright) session** covering both an iPhone 13 emulated viewport and a 1280px desktop viewport: register → create a shopping list → quick-add a plain item → expand the advanced panel and add a priced/quantified item (confirmed the finance summary's total, `price × quantity` math unchanged) → collapse/expand the budget summary via tap → open the item-actions sheet via the kebab and via a title tap → toggle urgent from the sheet (badge appears, rose border applied) → delete via the sheet (undo toast still fires) → open the FAB's bottom sheet, add an item, confirm it auto-closes and the form moves back inline → add an item with a URL and confirm the inline 🔗 icon renders with the right `href` and that "Ouvrir le lien" is un-hidden in the actions sheet for it → confirm on the desktop viewport that the FAB and kebab are both hidden and the pencil/trash buttons are visible instead. Zero console errors/page errors/failed requests across the whole run. **Not yet done**: no real mobile device or physical touch/gesture testing (Playwright's viewport emulation only); todo-list and custom-list card rendering (no checkbox/no quantity/no price fields) and a recurring item's badge alongside the new secondary row weren't specifically exercised in this pass, only reasoned about from the unchanged `FIELD_VISIBILITY_BY_TYPE`/`buildRecurrenceBadge` logic; and the product-thumbnail case (`buildItemThumbnail`, which the redesign didn't otherwise touch) wasn't re-verified since it needs a real scraped image, the same pre-existing gap noted elsewhere in this log.

- **"Keep last page on launch" preference**: Trakka previously always landed on the dashboard's "Lists" tab on every reload/relaunch, regardless of what was last open. `users.keep_last_page` (`INTEGER NOT NULL DEFAULT 1`, migration `internal/db/migrations/0010_keep_last_page_preference.sql`) is a per-user, per-account (not per-browser) on/off preference for this, defaulting to enabled; `models.User.KeepLastPage` carries it through `GET /api/v1/me` (both `internal/db.GetUser` and `getUserWithCredentials`'s queries were extended to select it, so it's present via every path that resolves the authenticated user — including `RequireSession`'s per-request `ValidateSession` → `GetUser` call), and a new `PATCH /api/v1/me` (`internal/handlers.handleMeUpdate`, `internal/db.UpdateUserKeepLastPage`) sets it, following the same `*bool`/"absent = untouched" partial-update convention as `PATCH /api/v1/items/{id}`. Deliberately **not** modeled as a `system_settings` row (that table is instance-wide, admin-only — see the "Custom categories" bullet's `user_id`-scoping reasoning above for why a genuinely per-user preference doesn't belong there) and deliberately **not** used to store the actual last-visited page itself — only the on/off choice lives server-side; which tab or list to reopen is tracked purely client-side, per the existing `HOUSE_STORAGE_KEY`/theme/language `localStorage`-only convention (see "Frontend (PWA, `static/`)" in Project conventions below), since it's meaningless to sync across devices/browsers the way an actual account preference is.

  The frontend has two pieces. First, a new user-facing "Paramètres" panel (distinct from the admin-only "Paramètres du Système" one in `admin.js`, and visible to every signed-in user rather than gated) — a new header button (`#user-settings-button`, a cog SVG rather than an emoji so it doesn't visually collide with the admin panel's ⚙️) opens `#user-settings-modal` (`static/js/settings.js`, mirroring `admin.js`'s open/close/Escape/click-outside/status-message wiring almost verbatim), currently holding just one checkbox toggle that reads from and PATCHes `/api/v1/me`. Second, the actual save/restore machinery lives in `static/js/app.js`: `LAST_VIEW_STORAGE_KEY` (`'trakka:lastView'`, JSON — either `{ type: 'tab', tab: 'planning' }` or `{ type: 'list', id: 42 }`) is written by `saveLastView`, called from the three places that change what's visible — `list_view.js`'s `selectList`/`showDashboard` and `planning.js`'s `setActiveTab` — and read back by `restoreLastView`, called once at the very end of `init()` after `state.currentUser`/`state.currentHouseId`/the dashboard are all resolved. `isKeepLastPageEnabled()` prefers `state.currentUser.keep_last_page` (the authoritative server value) but falls back to a `KEEP_LAST_PAGE_STORAGE_KEY` (`'trakka:keepLastPage'`) `localStorage` mirror — kept in sync by `setKeepLastPagePreference`, called both right after `/me` resolves in `init()` and right after a successful `PATCH` in `settings.js` — so the very first paint of a reload can decide whether to even attempt a restore without blocking on a network round-trip that might not resolve yet (e.g. while offline), the same reasoning `apiRequest`'s existing `navigator.onLine` short-circuit already uses elsewhere. `selectList` gained an `opts.silent` flag (used only by `restoreLastView`) so reopening a list that's since been deleted or made inaccessible fails quietly back to the dashboard instead of greeting a relaunch with an error banner; every ordinary caller (clicking a list card) is unaffected. No `sw.js`/IndexedDB changes were needed — this is pure client-side state layered on already-existing pieces (the `/me` response, `localStorage`), not a new offline-sync concern; `sw.js`'s `SHELL_CACHE`/`RUNTIME_CACHE` were bumped to `v25` purely to pick up the new `/js/settings.js` file and the modal/button markup added to the already-cached `index.html`.

  Verified: a new `internal/db/users_test.go` test (`TestKeepLastPagePreference` — defaults to `true` for a new user, `UpdateUserKeepLastPage` round-trips through both `GetUser` and `GetUserByEmail`, and updating a nonexistent user returns `ErrNotFound`), `go build`/`go vet`/`gofmt -l .`/`go test ./...` all clean; a live curl walkthrough of `GET`/`PATCH /api/v1/me` (default `true`, toggling to `false` and back, an empty-body PATCH being a no-op, an unknown-field PATCH correctly `400`-rejected by the existing strict-JSON decoder, and unauthenticated `401`); and — unlike many entries in this log — **two real headless-Chromium (Playwright) sessions** exercising the actual restore behavior end-to-end: logging in, opening the new settings modal and confirming the toggle reflects the server default, navigating to the "Urgent" tab and confirming `localStorage['trakka:lastView']` updates, reloading and confirming the app reopens directly on the Urgent tab (not the dashboard), creating and opening a list and confirming a reload reopens directly into that list's detail view, then disabling the preference via the modal and confirming a subsequent reload goes back to ignoring the saved view and lands on the dashboard as before — plus a second session confirming the modal's title/label/hint and the header button's `aria-label` all render correctly in both French and English via the language switcher. Zero console errors/page errors/failed requests across both runs. **Not yet done**: no verification on a real mobile device (same caveat as most frontend work in this log — only Playwright's desktop-viewport emulation was exercised, though the modal reuses the same responsive modal/toggle patterns already shipped and verified elsewhere); no way yet to reset this preference from the admin panel if a user gets stuck (not requested, and `is_admin` intentionally can't reach another user's own preferences); and restoring into a list that belongs to a house other than the currently-selected one was reasoned about (`selectList` fetches `/lists/{id}` directly, independent of `state.currentHouseId`) but not specifically clicked through with two houses in play.

- **Pinning shared lists to the dashboard**: a recipient of a directly-shared list can now pin it so it shows up on their own dashboard alongside their house's own lists, instead of only in the "Partagé avec moi" tab — see the dedicated "Pinning shared lists to the dashboard" bullet in the Architecture section above for the full design (`list_shares.is_pinned_to_dashboard`, migration 11, `PATCH /api/v1/lists/{id}/share/pin`, why it's scoped to `list_shares` only and not `space_shares`, the client-side dashboard merge in `app.js`'s `loadDashboard`/`shares.js`'s `loadPinnedSharedLists`). New/changed backend: `internal/db/migrations/0011_pin_shared_lists.sql`, `internal/db/shares.go` (`SetListSharePinned`, and every existing `list_shares` read/write updated to carry the new column), `internal/handlers/shares.go` (`handleListSharePin`), one new route in `internal/handlers/app.go`. Frontend: `app.js` gained a `PIN_ICON_SVG` constant and a 📌/📌-filled pin/unpin button in `buildListCard` (shown only for `list.access_source === 'list_share'`), `toggleListPin`, and the dashboard merge in `loadDashboard`; `shares.js` gained `loadPinnedSharedLists` and now also carries `is_pinned_to_dashboard` through `loadSharedView`'s per-list merge so the "Partagé avec moi" tab's own pin button reflects the right state. `sw.js`'s existing share-endpoint regex (`^\/api\/v1\/(custom-categories|lists)\/[^/]+\/share(\/[^/]+)?$`) already matched `/share/pin` with no changes needed, so the new endpoint correctly returns a `503` "requires connectivity" response while offline, same as granting/revoking a share (`SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v26` for the content-only changes to already-cached files). Documented in [docs/API.md](docs/API.md#patch-apiv1listsidsharepin) and [docs/DATABASE.md](docs/DATABASE.md#space_shares-and-list_shares). Verified: two new `internal/db/shares_test.go` cases (`TestSetListSharePinned` — defaults to unpinned, toggles on and back off, `ErrNotFound` for a user with no `list_shares` row at all; and `TestListSharedListsForUser` extended to confirm a pinned direct share comes back with `IsPinnedToDashboard: true` while a Space-only share never does) — `go build`/`go vet`/`gofmt -l .`/`go test ./...` all clean — plus a full live curl walkthrough: sharing a list, confirming it defaults to `is_pinned_to_dashboard: false`, pinning it and confirming `?shared_with_me=true` reflects `true`, unpinning back to `false`, a `404` when the caller (the list's own owner, who holds no `list_shares` row on their own list) tries to pin it, and a `400` on a request body missing `pinned`. **Not yet done**: no browser verification of the frontend (the 📌 button's appearance/toggle on a card in the "Partagé avec moi" tab, a pinned card actually appearing on the dashboard in the right grid with the 👥 badge, unpinning from the dashboard card itself) — no browser automation tooling was available in the sandbox this was built in, same caveat as most other frontend-only work in this log.

  A follow-up bug report claimed this endpoint rejected a share recipient with `403 "not a member of this house"`. Re-verified from scratch against the code as shipped above (fresh database, a recipient in a completely unrelated House, `UserCanAccessHouse` asserted `false` before pinning) and the report did not reproduce: `handleListSharePin` never called `authorizeHouseAccess` to begin with, exactly per the design described above. Added `internal/handlers/shares_test.go`'s `TestHandleListSharePinDoesNotRequireHouseMembership` as a permanent regression guard either way — it calls the handler directly (bypassing `Routes()`'s mux, since only the handler's own authorization logic is under test) with a caller who has zero House relationship to the list, asserts `200`, and separately asserts the House *owner* (full membership, but no `list_shares` row of their own) gets `404` — proving the check really is "holds a share", not "is a House member" in either direction. Also strengthened `db.ListSharedListsForUser`'s doc comment to spell out that its one House-related SQL condition (`lists.house_id NOT IN (...)`) is a dedup exclusion, never an access requirement — every row is sourced by a `shared_with_user_id = ?` match.

  **A second report, with actual server logs, pointed at a real bug — but not the endpoint above.** The logs showed `GET /api/v1/lists` and `GET /api/v1/price-alerts` both `403`ing immediately on an ordinary dashboard reload (no `PATCH .../share/pin` anywhere in them), followed a few milliseconds later by `GET /api/v1/houses` `200` and the *same* two endpoints succeeding — a transient race, not a permanent authorization bug, but `loadDashboard()`'s error handling treats a `403` as a genuine failure (unlike a network error) and calls `showError()`, so it briefly flashed the same "not a member of this house" text as a banner right on the dashboard. Root cause: `state.currentHouseId` is only *authoritative* once `loadHouses()` has validated it against a live `GET /api/v1/houses` — before that it can hold whatever `hydrateFromCache()` derived from the IndexedDB mirror (itself possibly stale, e.g. left over from a different account previously signed into the same browser). Several independent triggers can call `loadDashboard()`/`loadNotifications()` — the initial `init()` sequence itself, `i18n.js`'s `trakka:lang-changed` event (dispatched on every `loadLang()` call, **including the very first, page-bootstrap one** — not just a genuine user-initiated switch), the `visibilitychange`/`online` listeners, and a `trakka-sync-complete` message from the service worker — and none of them coordinated with each other about whether `loadHouses()` had actually resolved yet.

  Fixed with two complementary changes, both in this session: (1) `static/js/i18n.js`'s `loadLang()` now only dispatches `trakka:lang-changed` from its *second* call onward (a new `hasLoadedOnce` flag) — the very first call, at page-load bootstrap, has nothing dynamic to re-render yet (`applyTranslations()` already handles the static `data-i18n` markup unconditionally), so reacting to it was pure downside; a genuine mid-session switch via the header dropdown is completely unaffected, since `hasLoadedOnce` is already `true` by the time a user can click it. (2) The deeper, structural fix: `static/js/app.js` gained `ensureHousesLoaded()` — a promise-memoized gate (`housesLoadedOnce`/`housesLoadingPromise`) that `loadDashboard()` now awaits at its very top, and that `init()` calls instead of `loadHouses()` directly — so `state.currentHouseId` is *guaranteed* validated against the live server before any house-scoped fetch fires, no matter which of the independent triggers calls it first or how they're timed relative to each other. `notifications.js`'s `loadNotifications()` (the other `state.currentHouseId`-scoped loader, called from the same set of triggers) awaits the same gate. `planning.js`/`urgent.js`/`spaces.js`/`shares.js`'s own tab loaders needed no equivalent change: none of them can become the active tab before `restoreLastView()` runs at the tail of `init()`, by which point `ensureHousesLoaded()` has already resolved via `loadDashboard()`'s own call earlier in the same sequence.

  Verified with real headless-Chromium (Playwright) sessions rather than just reasoning about the code — this bug specifically motivated reaching for actual browser automation instead of static analysis, given how much timing determines whether it manifests. Three separate reproduction attempts against the *unfixed* code (switching accounts in the same browser storage; sharing+pinning a list then reloading 25 times in a row with `fetch` instrumented to capture a stack trace on every call; and literally killing and restarting the server process mid-session before reloading, matching the report's own description) all failed to trigger the exact race even once — headless Chromium's IndexedDB/fetch scheduling on localhost is apparently fast and consistent enough that the specific interleaving the logs showed doesn't reproduce this way, so the precise trigger among the four candidates remains unconfirmed. The `ensureHousesLoaded()` gate doesn't depend on pinning down which one it was, though: it's correct by construction (a single-flight promise every caller awaits) and eliminates the entire class of "house-scoped fetch fires before `state.currentHouseId` is validated" races regardless of which trigger or timing caused this specific report. Separately confirmed via Playwright that a genuine, user-initiated language switch (clicking the header's FR/EN dropdown) still correctly re-renders dynamic content and shows no banner, i.e. the `i18n.js` change didn't regress the feature it's part of. `go build`/`go vet`/`gofmt -l .`/`go test ./...` all clean (no Go files touched), `node --check` on every edited JS file. `sw.js`'s `SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v27` for the content-only changes to already-cached `app.js`/`i18n.js`/`notifications.js`. **If this resurfaces**: capture the exact `fetch` call (URL including query string — the server's own request log only records `r.URL.Path`, not the query string, so a bare `"path":"/api/v1/lists"` log line can't by itself distinguish the house-scoped call from the shared-with-me one) and, ideally, a stack trace (the instrumentation approach in this session's Playwright scripts — monkey-patching `window.fetch` via `context.addInitScript` before any page script runs — is the fastest way to get one) to finally identify which of the four trigger points is actually responsible.

- **Mobile-accessible pin/unpin option for shared lists**: a follow-up request asked for the pin/unpin toggle above to also be reachable as a dedicated, text-labeled option — not just a bare icon — and specifically to work well on a narrow screen, mirroring `list_view.js`'s existing item-row pattern (a `[⋮]` kebab opening a bottom sheet on mobile, direct icon buttons on desktop) rather than introducing a different interaction. `buildListCard`'s `list.access_source === 'list_share'` branch (`static/js/app.js`) now splits its pin button in two: the existing 📌 icon button stays but is now `hidden md:flex` (desktop only), and a new `[⋮]` kebab button (`flex md:hidden`, `KEBAB_ICON_SVG` — defined in `list_view.js`, referenced here the same way `PENCIL_ICON_SVG` already was, since it's only read at render/click time after every script has loaded) opens a new `#list-card-actions-sheet` bottom sheet on mobile, styled and wired exactly like `#item-actions-sheet` (a dialog with a title set to the list's name and one text-labeled action row). The sheet's single option relabels itself based on state — `📌 Épingler sur le tableau de bord` when unpinned, `📍 Retirer du tableau de bord` when already pinned (`openListCardActionsSheet` swaps both the emoji and the `modals.listActions.pin`/`unpin` i18n text) — and its click handler just closes the sheet and calls the same `toggleListPin(list)` the desktop icon button already used, so both entry points share one code path and one `PATCH /api/v1/lists/{id}/share/pin` call; `refreshVisibleView()` (unchanged) is what re-renders the dashboard/"Partagé avec moi" tab in place afterward, with no full page reload either way. Since `shares.js`'s "Partagé avec moi" tab renders through this same `buildListCard`, the kebab/sheet/desktop-icon split and the new badge below are automatically present there too, not just on the dashboard grids — satisfying the "works in both the shared-lists view and the dashboard, mobile and desktop" requirement without a second implementation. Separately, a card now also carries visible feedback independent of the button's own state: a small `📌 Épinglée` badge (new `amber` entry in `badge()`'s color map) appears in the card's top badge row, next to the existing `👥 Partagé` one, whenever `list.is_pinned_to_dashboard` is true — so a pinned card is recognizable at a glance even before hovering/tapping its action control. New `common.listActionsAriaLabel`, `shares.pinnedBadge`, and `modals.listActions.{pin,unpin}` i18n keys added to both `static/locales/{fr,en}.json`. No backend or `sw.js` write-path changes were needed (the endpoint and its offline `503` behavior already existed); `SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v32` for the content-only changes to already-cached `index.html`/`app.js`/the locale files. Verified: `go build`/`go vet ./...`/`gofmt -l .` clean (no Go files touched), `node --check` on the edited JS, and a Python `html.parser` pass confirming `index.html`'s tags still balance. **Not yet done**: same as the base pinning feature above — no browser verification of either the kebab/sheet on a narrow viewport or the new badge, no browser automation tooling was available in the sandbox this was built in. Before relying on it: on a narrow/mobile viewport, confirm the 📌 icon is hidden and the `[⋮]` kebab is shown instead on every shared-list card (dashboard grids and the "Partagé avec moi" tab alike), tapping it opens the sheet with the list's name as its title and the correctly-worded pin/unpin option, tapping that option closes the sheet and toggles the pin (card moves on/off the dashboard, badge appears/disappears) without a page reload; on a wide/desktop viewport confirm the kebab is hidden and the icon button alone still works as before.

- **Pinning fixed and extended to cover shared Spaces, not just directly-shared Lists**: a bug report found that a List reached *only* through a shared Space (no `list_shares` row of its own — see "Pinning shared lists" above) could not be pinned at all: `PATCH /api/v1/lists/{id}/share/pin` (`SetListSharePinned`) only ever `UPDATE`d an existing `list_shares` row, so it returned `404` for exactly this case, and there was no way to pin a shared Space as a whole either. Both are fixed/added in this session — see the rewritten "Pinning shared lists (and shared Spaces) to the dashboard" bullet in the Architecture section above for the full design; this entry just records what changed and how it was verified. Backend: `internal/db/migrations/0012_pin_shared_spaces.sql` adds `space_shares.is_pinned_to_dashboard`; `internal/db/shares.go` gained `spaceSharePermissionForList` (a `lists JOIN space_shares` lookup) and `SetSpaceSharePinned`, and `SetListSharePinned` now falls back to an `INSERT ... ON CONFLICT` auto-create when the plain `UPDATE` affects zero rows; `ListSharedListsForUser`'s `space_share` branch now selects `ss.is_pinned_to_dashboard` instead of a hardcoded `0`; `ListSpacesSharedWithUser` is a new query (Space-level equivalent of `ListSharedListsForUser`) backing a new `?shared_with_me=true` mode on `GET /api/v1/custom-categories` (`internal/handlers/categories.go`); `internal/handlers/shares.go` gained `handleSpaceSharePin`, routed at `PATCH /api/v1/custom-categories/{id}/share/pin` in `internal/handlers/app.go` — this path (nesting under the existing `/custom-categories/{id}` resource) was chosen over the `/api/v1/spaces/shares/:id/pin` shape floated in the request that prompted this, to stay consistent with every other Space-sharing route already living under `/custom-categories/{id}/share...` rather than introducing a second URL root for the same resource. Frontend: `static/js/app.js`'s `buildListCard` now shows the pin icon/kebab for `list.access_source === 'space_share'` too (previously `'list_share'` only), so an individual List reached via a shared Space can be pinned the same way a directly-shared one already could; `static/js/spaces.js` gained `sharedCategories`/`sharedCategoryLists` state, `loadSharedCustomCategories`/`loadSharedCategoryLists`, a new "Espaces partagés avec moi" section (`buildSharedCategorySection`) in the "Espaces" tab listing every Space shared with the caller regardless of pin state (there has to be *some* place to discover and pin one for the first time), and a `[⋮]` kebab opening a new `#space-card-actions-sheet` bottom sheet (mirroring `#list-card-actions-sheet` almost exactly) for the pin/unpin toggle (`toggleSpacePin`) — unlike `toggleListPin`'s `refreshVisibleView()`, this explicitly calls both `refreshSpacesIfActive()` and `loadDashboard()` regardless of which tab is active, since `setActiveTab('dashboard')` doesn't itself trigger a refetch (only `showDashboard()` does) and the whole point of pinning a Space is for its lists to appear the moment the user next looks at the dashboard, not only after some unrelated trigger refreshes it. `static/sw.js` excludes `?shared_with_me=true` custom-categories from the plain categories IndexedDB mirror (mirroring the existing lists exclusion) and answers it with an empty array while offline; `SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v33`. New `modals.listActions.{pinSpace,unpinSpace}` and `spaces.{sharedSectionTitle,actionsAriaLabel}` i18n keys in both `static/locales/{fr,en}.json`. A documented, intentional interaction rather than a bug: pinned state is combined via OR across the two levels, so unpinning one List individually while its parent Space is still pinned as a whole leaves the card on the dashboard regardless — there is currently no "pin the Space but exclude this one List" option, and clicking that List's own unpin control in that situation will look like a no-op from the dashboard's point of view even though the underlying `list_shares` row did flip. Verified: five new `internal/db` tests (`TestSetSpaceSharePinned`, `TestSetListSharePinnedViaSpaceAccess` — the core regression case, confirming the auto-created row mirrors the Space's permission and leaves `AccessLevelForList` unchanged — `TestListSpacesSharedWithUser`, `TestListSharedListsForUserSpacePinPropagates`, plus the pre-existing `TestListSharedListsForUser`/`TestAccessLevelForList` re-run unchanged) and a new `internal/handlers` test (`TestHandleSpaceSharePinDoesNotRequireOwnership`, mirroring `TestHandleListSharePinDoesNotRequireHouseMembership`) — `go build`/`go vet`/`gofmt -l .`/`go test ./...` all clean. Additionally verified with a full live two-user curl walkthrough against a real running server (a throwaway `DB_PATH`, register two accounts, share a category with the second one): confirmed `PATCH /api/v1/lists/{id}/share/pin` on a list reached *only* via the shared Space — previously `404`, the exact bug this session fixes — now returns `200` and auto-creates a `list_shares` row with the Space's own `write` permission; confirmed pinning the whole Space via `PATCH /api/v1/custom-categories/{id}/share/pin` makes *both* of its lists (one individually unpinned-but-directly-shared, one reachable only via the Space) come back `is_pinned_to_dashboard: true` from `?shared_with_me=true` with no per-list action; confirmed unpinning the Space reverts both; and confirmed the edge cases — `404` for the Space's own owner (holds no `space_shares` row of their own), `400` for a request body missing `pinned`, `404` for a nonexistent category id, `401` unauthenticated. **Not yet done**: no browser verification of the frontend (the "Espaces partagés avec moi" section, the space kebab/sheet, a pinned Space's lists actually appearing on the dashboard grids) — no browser automation tooling was available in the sandbox this was built in, same caveat as most other frontend-only work in this log.

- **Rename house**: `PUT /api/v1/houses/{id}` (owner-only, `internal/handlers.handleHousesUpdate`/`internal/db.UpdateHouse`) already existed and was already documented ([docs/API.md](docs/API.md#put-apiv1housesid)) from the original houses feature — every session touching this has only ever needed a frontend surface for it, no backend change. The UI went through two iterations in the same handoff window, worth recording because the first one was explicitly rejected as poor UX: a first pass tucked the rename control inside the "Membres de la Maison" modal (a pencil next to a house-name row there), which a follow-up request called out as too hidden — renaming isn't a membership operation and shouldn't require opening that modal first. It was replaced outright (not layered on top of) by an inline, one-click control directly on the dashboard header, which is what ships now: `#house-toolbar` (the `Maison` label + `#house-select` + a ✏️ pencil button, `#rename-house-inline-button` — same pencil path as `list_view.js`'s `PENCIL_ICON_SVG`/the list-detail header's edit button — + the "Membres" button) and a sibling `#rename-house-inline-form` are mutually exclusive siblings in `static/index.html`; toggling one `hidden` and the other visible (`openRenameHouseInline`/`closeRenameHouseInline` in `app.js`) swaps the row between "browse" and "edit" in place, no modal at all. The pencil is visible only when `currentHouseRole() === 'owner'` (`updateRenameHouseButton()`, called everywhere `updateManageMembersButton()` already was — `loadHouses`, `selectHouse`, `hydrateFromCache` — so it never lags behind whatever role state the rest of the header reflects); clicking it prefills `#rename-house-inline-input` with the current name and focuses/selects it, and the form carries a green ✓ "confirm" button and a rose ✕ "cancel" one (Feather-style check/X paths) rather than text buttons, keeping the whole interaction to a single click plus Enter (or a tap on ✓). Escape while the form is open closes it back to browse mode (a dedicated `keydown` listener guarded by `isRenameHouseInlineOpen()`), and `selectHouse()` also force-closes it defensively (switching houses mid-edit isn't reachable through the UI since the toolbar, including `#house-select`, is hidden while editing, but this keeps state consistent regardless). Submitting `PUT`s the trimmed name, patches `state.houses` in place and re-populates `#house-select` so the dropdown reflects the new name immediately (no full `loadHouses()` round trip), then shows `static/js/undo.js`'s `TrakkaToast.success(message)` (a sibling to the existing `TrakkaUndo.schedule` countdown/undo toast added in the first pass and kept as-is — reuses the same `#toast-container`, no undo button or countdown bar, since a rename is already committed by the time it shows). New `dashboard.rename{HouseAriaLabel,ConfirmAriaLabel,CancelAriaLabel,Success}` i18n keys in both `static/locales/{fr,en}.json` (the first pass's now-dead `modals.members.rename*` keys were removed rather than left stale). No `sw.js` write-path changes were needed either time — `PUT /api/v1/houses/{id}` was already generically mirrored into the IndexedDB houses store and already correctly returns a `503` "requires connectivity" response while offline (see "Houses are read-mirrored but not offline-writable" in [docs/PWA.md](docs/PWA.md)); `SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v29` for the content-only changes to already-cached `index.html`/`app.js`/the locale files. Verified: `go build`/`go vet ./...`/`gofmt -l .` clean (no Go files touched), `node --check` on the edited JS, and a Python `html.parser` pass confirming `index.html`'s tags still balance. **Not yet done**: no browser verification of either iteration — no browser automation tooling was available in the sandbox this was built in, same caveat as most other frontend-only work in this log. Before relying on it: as a house's owner, confirm the pencil appears next to the selector, click it, edit the name, and confirm the header selector updates immediately along with the success toast; as a non-owner member, confirm the pencil stays hidden; confirm Escape and the ✕ button both cancel cleanly back to the selector without saving; and confirm a `PUT` attempt while offline surfaces the existing "requires connectivity" error rather than silently queuing.

- **Pinning: dashboard didn't refresh without a reload, and a House-visible (not owned, not explicitly shared) Space couldn't be pinned at all**: two more bug reports against the pinning feature above. (1) **No dynamic refresh**: pinning/unpinning a shared List or Space correctly wrote to the database, but the Dashboard tab often kept showing stale content until a full page reload. Root cause: of the five dashboard tabs, only `setActiveTab('dashboard')` (`static/js/planning.js`) skipped refetching on click — `planning`/`urgent`/`spaces`/`shared` all called their own loader when clicked, but clicking back to "Tableau de bord" via the tab button just toggled visibility, relying on `showDashboard()` (used only by the logo link) or some earlier, possibly-stale `loadDashboard()` call. Fixed by adding `if (tab === 'dashboard') loadDashboard();` alongside the other four, and a new `isDashboardTabActive()` helper (mirroring `isPlanningTabActive`/etc.) so `app.js`'s `toggleListPin` can eagerly re-run `loadDashboard()` after a pin/unpin without double-fetching when already on that tab (`toggleSpacePin` in `spaces.js` already unconditionally called `loadDashboard()`, since it's only ever invoked from the Espaces tab). Both toggle functions also gained a `TrakkaToast.success(...)` confirmation (new `shares.{pinnedToast,unpinnedToast}`/`spaces.{pinnedToast,unpinnedToast}` i18n keys), mirroring the existing house-rename toast, since pinning from a card that isn't even on screen (e.g. the "Partagé avec moi" tab) previously gave no feedback at all. (2) **A Space visible only through House membership couldn't be pinned**: the existing pin endpoint (`handleSpaceSharePin`) only ever recognized an explicit `space_shares` grant — a fellow House member who could see a Space only because someone else in the *same* House tagged one of its own lists with their personal Space (no share, no ownership) got a `404` and had no way to discover the Space as a first-class object at all, even though `GET /api/v1/lists`/`{id}` already embedded its `custom_category` on that shared list per the "Custom categories" design. Fixed with a new access path, parallel to `space_shares` rather than replacing it: `internal/db/migrations/0013_pin_house_spaces.sql` adds `space_house_pins` (`custom_category_id`, `user_id`, `UNIQUE` on the pair) — no `permission` column, since House membership already fully determines the underlying lists' access via `AccessLevelForList`, this table exists purely to record the pin preference; row presence means pinned, so `db.SetSpaceHousePinned` unpins via `DELETE` rather than flipping a flag. `db.spaceAccessibleViaHouse(categoryID, userID)` is the new "is this category tagged on a list in one of userID's Houses" check — it explicitly excludes the category's own owner (`cc.user_id != ?`), since an owner is typically also a House member of wherever their own Space is used and would otherwise be able to "pin" their own already-visible Space for no purpose (caught by a db-layer and a handler-layer regression test, not by the original implementation — see [docs/DATABASE.md](docs/DATABASE.md#space_house_pins)). `handleSpaceSharePin` now tries `SetSpaceSharePinned` first and only falls back to `SetSpaceHousePinned` on `ErrNotFound`, returning a new lightweight `models.SpacePinStatus` (`{custom_category_id, is_pinned_to_dashboard, access_source: "house_member"}`) in the fallback case since there's no `SpaceShare` row to return. `db.ListSpacesSharedWithUser` was renamed `ListSpacesVisibleToUser` and now `UNION ALL`s a second source into its query — every category the caller doesn't own that's tagged on a list in one of their Houses, `LEFT JOIN`ed against `space_house_pins` for the pin flag — so `GET /api/v1/custom-categories?shared_with_me=true` (unchanged endpoint, broadened meaning) now returns both `access_source: "space_share"` and `"house_member"` rows; `CustomCategory` gained an `AccessSource` field to carry it (mirroring `List.AccessSource`). A new `db.ListPinnedHouseSpaceLists` + `GET /api/v1/lists?pinned_house_spaces=true` (a third, separate mode from `?shared_with_me=true` — deliberately not unioned into that query, since `ListSharedListsForUser`'s own House-membership exclusion would filter every `house_member`-sourced row straight back out, being the exact opposite condition) is what actually surfaces a pinned House Space's lists on the dashboard, including from a House other than the one currently selected. Frontend: `static/js/app.js`'s `loadDashboard` now merges three sources (own-House listing, pinned shares, pinned House-space lists) deduped by list id (a `house_member`-sourced list is often already present via the caller's own currently-selected House, in which case pinning's only visible effect is the 📌 badge — its cross-House value shows once a *different* House is selected instead), and `buildListCard`'s pin/kebab branch now also covers `access_source === 'house_member'`; `static/js/spaces.js`'s "Espaces" tab gained a second heading, "Espaces de la Maison" (new `spaces.houseSectionTitle`/`houseBadge` i18n keys, a 🏠 badge distinct from the 👥 "Partagé" one), sourced from the same broadened `sharedCategories` array split by `access_source`, with its lists drawn from `spacesLists` (current House) plus a new `houseSpaceLists` fetch (cross-House pinned ones) rather than `sharedCategoryLists` (which, per the exclusion above, never contains a `house_member`-sourced list). `static/sw.js` gained the same offline exclusion/empty-array-while-offline treatment for `?pinned_house_spaces=true` that `?shared_with_me=true` already had (`SHELL_CACHE`/`RUNTIME_CACHE` bumped to `v34`). Verified: nine new/changed `internal/db` tests (`TestSpaceAccessibleViaHouseMemberPin` including the owner-exclusion case, `TestListSpacesVisibleToUserHouseMember`, `TestListPinnedHouseSpaceLists`, `TestListSpacesVisibleToUser` renamed/extended) and two new `internal/handlers` tests (`TestHandleSpaceSharePinFallsBackToHouseMembership` including its own owner-exclusion assertion) — `go build`/`go vet`/`gofmt -l .`/`CGO_ENABLED=1 go test -race ./...` all clean. Unlike most pinning-related entries in this log, **this one was verified with real headless-Chromium (Playwright) sessions against the actual built server**, not just curl/unit tests, specifically because the "no reload needed" claim is exactly the kind of timing-dependent behavior static analysis can't confirm: three scripted browser runs (a two-user same-House scenario; a three-user cross-House scenario — the fellow member's own House selected in the dropdown, the pinned Space's list living in a completely different House they also belong to; and a plain direct-List-share pin from the "Partagé avec moi" tab) all confirmed, via the real tab buttons and kebab/bottom-sheet UI, zero console/page errors, a toast on pin, the "Espaces de la Maison" section rendering with the correct badge, a House-visible Space's pin/unpin correctly toggling its cross-House lists on and off the dashboard with **no page reload**, and the same for the pre-existing List-share pin flow this session's tab-refresh fix also covers. **Not yet done**: no verification on a real mobile device/touch input (Playwright viewport emulation only, and only a desktop viewport was exercised this time — the narrow-viewport kebab/sheet variant for a House-visible Space's card wasn't specifically re-checked, though it reuses the identical `#space-card-actions-sheet` already verified for the `space_share` case above).

### What's left / open points

- **Rename house has never been exercised in a browser** — see its own bullet under "What's done" for the exact scenario to click through: as an owner, click the ✏️ pencil next to the dashboard's house selector, confirm it swaps to an inline text input with ✓/✕ buttons prefilled with the current name, save and confirm the selector updates immediately along with a success toast, and confirm Escape/✕ both cancel cleanly; as a non-owner member, confirm the pencil stays hidden.
- **Pinning shared lists and shared Spaces: the core "pin → appears without reload, unpin → disappears without reload" flow is now browser-verified** (direct List share, and a House-visible Space both same-House and cross-House — see the "Pinning: dashboard didn't refresh..." bullet above for the three Playwright scenarios actually run), but the mobile kebab/bottom-sheet variant was only exercised at a desktop viewport this time — **still not checked on a narrow viewport**: confirm the desktop pin icon is hidden in favor of a `[⋮]` kebab/bottom-sheet on a shared List card (dashboard grids and "Partagé avec moi" tab alike) and on a Space card in either "Espaces partagés avec moi" or the newer "Espaces de la Maison" section, and that tapping through each sheet's pin/unpin option behaves identically to the desktop icon button already confirmed. Also still unconfirmed in a browser: pinning a whole *explicitly-shared* (`space_share`, not `house_member`) Space causing every List reachable through it to appear at once, and the "unpinning one List individually can't override its parent Space being pinned as a whole" interaction — only the `house_member` pin path's own dashboard-appearance behavior was scripted this session.
- **The redesigned list detail view needs a pass on real hardware and the less-common list types** — see the "List detail view redesigned Mobile-First" bullet above. It was verified end to end in headless Chromium (Playwright) across a mobile and a desktop viewport with zero console errors, which is more than most frontend-only entries in this log can say, but still: no physical phone/real touch or gesture testing, no click-through of a `todo` or `custom` list's cards (no checkbox/no quantity/no price fields — should already work from the unchanged `FIELD_VISIBILITY_BY_TYPE` gating, but wasn't specifically clicked through), and no re-check of a recurring item's 🔄 badge or a product thumbnail alongside the new secondary row/top-row split.
- **The mobile/PWA login-loop fix above (`SameSite=Lax` session cookie, explicit `Max-Age`, explicit `credentials: 'same-origin'`) has never been exercised on a real mobile device** — see its own bullet under "What's done" for the exact scenario to verify (install as an iOS home-screen PWA if possible, log in, confirm `GET /api/v1/me` succeeds immediately after the post-login redirect rather than 401-looping back to `/auth/login`).
- **The item-quantity fix above (subtotal math + the new [-]/[+] stepper) has never been exercised in a browser** — see its own bullet under "What's done" for the exact scenario to click through (price × quantity in the row subtotal, the finance summary bar, the dashboard/Espaces card total badge, and the planning view's totals, plus the stepper's rapid-click/rollback behavior).
- **The service worker update banner was never exercised in a browser** — see the dedicated "Versioned SQLite migration engine..." bullet above. The migration engine itself has real Go test coverage and `go test ./...` is clean, but `watchForServiceWorkerUpdate`/`#update-banner` in `static/js/app.js` needs a real deployment-and-reload cycle (bump `sw.js`'s cache version, redeploy, reload an already-open tab) to confirm the banner actually appears and that its button reloads onto the new assets — no browser automation tooling was available in the sandbox this was built in. Also confirm the `hasExistingSchema` adoption path against a real, full-sized pre-existing `/data/trakka.db` (only a synthetic minimal seed was tested) before relying on it for an actual upgrade.
- **Granular sharing's frontend was never exercised in a browser** — see the dedicated bullet above. The backend (schema, `internal/db`, `internal/handlers`, RBAC enforcement) was verified with `go test` and an extensive live curl walkthrough covering every access-control edge case, but `static/js/shares.js` (the share modal, the 👥 buttons on cards/sections/the list-detail header, the "Partagé avec moi" tab) has not been clicked through in an actual browser.

- **The offline UX pass above (Budget/Urgent/Espaces cache fallback + suppressed error banner) needs a real-browser pass** — see its own bullet above for the exact scenario to click through.
- **Custom categories' frontend was never exercised in a browser** — the "Espaces" tab, the create/edit/delete category modal, and the "new list" modal's category picker (`static/js/spaces.js`) were built but no browser automation tooling was available in the sandbox this was built in. Check the empty state, category create/edit/delete (including the undo toast), the nested "create a space from the list modal" flow, and the mixed-`type` list grouping in an actual browser before relying on it. (Reassigning an *existing* list's category, name, type, and icon after creation is now possible via the "List editing" feature above — this no longer needs a workaround through the Espaces tab alone.)
- **List editing's frontend was never exercised in a browser** — see the "List editing" bullet above. The backend (`lists.icon`, `PUT /api/v1/lists/{id}`) was verified with `go test`/`go vet`/`gofmt` and a live curl walkthrough, but the modal's create/edit toggle, the icon presets, the new ✏️ buttons on dashboard/Espaces cards and the list-detail header, and the icon actually rendering next to a list's name everywhere it should, have not been clicked through in an actual browser.

- **OIDC was never tested against a real identity provider.** The hand-rolled discovery/PKCE/JWKS/RS256-verification code builds and follows the spec as implemented, but no OIDC provider (Authelia, Keycloak, Google, ...) was available to actually exercise `/auth/oidc/login` → `/auth/oidc/callback` end-to-end in the sandbox that built it. Treat this path as unverified until it's been run against a real provider at least once — set up a local Authelia/Keycloak via Docker (or a Google OAuth test client) and walk through a full login before relying on it.
- **The Docker image build itself was not verified** for the same reason (no Docker daemon available) — neither the original `templates/` `COPY` addition nor the later distroless/`read_only` hardening has actually been built and run. Before deploying: `docker compose up -d --build` (or `podman-compose up -d --build`) and confirm the container starts cleanly, `/healthz` passes, and an item create/read round-trip works with `read_only: true` in effect — that's the setting most likely to surface a real problem (e.g. anything unexpectedly trying to write outside `/data`/`/tmp`), more so than the base-image swap itself.
- **No UI to rename/delete a house, or to leave a house you're a member of** — the dashboard exposes creating a house and managing membership (invite/remove), but `PUT`/`DELETE /api/v1/houses/{id}` and "leave house" (self-`DELETE` on `/members/{userId}`) exist and are documented but aren't wired to any dashboard button yet (they were already true of house rename/delete before this feature; "leave house" is new).
- **No account deletion/management UI or endpoint** — a user can't currently delete their own account, change their password, or change their email from within the app.
- **Nothing has been committed yet** beyond the initial commit (`1a74e2d Initial commit`): every project file shows up as untracked/modified in `git status`. Need to decide how to split the commits (one big commit per feature area, or several smaller logical commits) before pushing anything.
- **No test suite for most of the app**: `internal/scraper` and `internal/db/items_test.go` now have real Go unit tests (see the automatic-price-lookup entry above), but everything else still has none, and there are no JS tests (though [docs/PWA.md](docs/PWA.md) explains how to test `sw.js`/`db.js` outside a browser via `fake-indexeddb` + `vm` — that approach hasn't actually been set up in the repo yet). Given how security-sensitive the OIDC/session code is, that's a stronger candidate for coverage than most of the remaining app.
- **The automatic price & image lookup was never exercised against a real product page** — only against `example.com` (no price/image present, so only the "nothing found" path ran) and a rejected `127.0.0.1` (confirming the SSRF guard). Point it at a real Shopify/Amazon/Fnac-style listing at least once to confirm the OpenGraph/JSON-LD/microdata/Twitter-Card parsing actually matches something in the wild, not just the synthetic HTML fixtures in `scraper_test.go` — and check the thumbnail/lightbox UI (`buildItemThumbnail`, `#image-preview-modal` in `static/js/list_view.js`) in an actual browser, which also hasn't happened yet.
- **CalDAV/Radicale integration doesn't exist yet**: `compose.yml` can start a Radicale container (`calendar` profile), but Trakka doesn't talk to it — no `.ics`/CalDAV export of `todo` lists. `docs/DEPLOYMENT.md` explicitly describes this as "a separate, not-yet-implemented piece of work".
- **No TLS reverse proxy provided**: `compose.yml` serves plain HTTP by design (PWA/offline only work on `localhost` or behind TLS, see [docs/PWA.md](docs/PWA.md)) — putting a Caddy/Traefik/nginx in front is left to the final deployment, not included in this repo. This now matters more than before: `SESSION_COOKIE_SECURE` should stay `true` (the default) in any real deployment, which requires HTTPS in front for the session cookie to actually get sent.
- **The new CI/CD pipeline has never actually run on GitHub's runners** — `.github/workflows/ci.yml` was written, and every Go-side tool it calls (`golangci-lint`, `gosec`, `govulncheck`, `go test -race -cover`) was run locally against the current tree and confirmed clean, plus the YAML's structure (jobs/steps/permissions) was validated with a small standalone parser, but no push or PR was made in the sandbox that built this, so the Docker build, both Trivy scans, the SARIF uploads to the Security tab, and the build-provenance attestation are all unverified beyond local reasoning. Push a commit or open a PR and watch the Actions tab before trusting it fully — see the new "CI/CD & Sécurité" section above for the full pipeline design.
- **`templates/login.html` is still French-only** — it's server-rendered via `html/template` and never loads `i18n.js`, so the FR/EN toggle added this session doesn't reach the login/register page at all.
- **Several dynamic JS strings still aren't translated**: item counts, "Terminés (N)"/"Tout est terminé pour le moment.", API error messages, and the dashboard's various empty-state messages in `app.js`/`list_view.js` are still hard-coded French, unlike the parts of the UI the i18n session did convert (see the "FR/EN multilingual UI" bullet above for exactly what's covered).
- **No Go test coverage for `target_month`**: `internal/validate.Month` and the item handlers'/`internal/db`'s handling of it were only verified live (curl + two headless-Chrome sessions), not with `go test`. `internal/validate` has no tests at all yet (not even for the pre-existing `URL` validator), so adding a `validate_test.go` would be a good place to start.
- **The planning view always fetches every shopping list's items on open/house-switch/sync** (the same `?house_id=` + per-list fan-out `loadDashboard` already uses) rather than a dedicated server-side query — fine at the scale this app targets, but if a house ever has many shopping lists this is the first place to optimize (e.g. a `GET /api/v1/items?house_id=&from=&to=` endpoint).
- **Recurring items have no `EVERY_X_DAYS:<n>` UI and no due-date display** — the API and both the Go and JS advance logic accept/use it, but the dropdown only offers the four fixed cadences, and `due_date` is tracked purely as backend bookkeeping with nothing rendering it in the UI yet (per the original feature request's own scope). A future session could add either if wanted.
- **Urgent items' frontend was never exercised in a browser** — no browser automation tooling (Playwright or similar) was available in the sandbox that built it. Only the backend (Go tests + a live curl walkthrough) was verified. Check the checkbox in both the create form and edit modal, the 🚨 badge, the sort-to-top of unfinished urgent items within a list, and the "Urgent" dashboard tab's cross-list fan-out/mark-done/click-through-to-list before relying on any of it.
- **Price alerts / deal notifications were never verified live** — no curl walkthrough (no product page with an actual, triggerable price drop was available in the sandbox this was built in) and no browser verification of the 🔔 bell/badge/drawer. Before relying on it: (1) manually insert a `price_alerts` row (or point an item's `url` at a page you control and lower the listed price) and confirm `GET /api/v1/price-alerts?house_id=&status=pending` returns it, (2) accept one and confirm the item's `price`/`price_auto` update, reject another and confirm the item is untouched, (3) exercise the bell/badge/drawer in an actual browser, including the optimistic-with-rollback behavior on a forced request failure. Also consider trying `POST /api/v1/items/{id}/price-check` against a real listing to confirm the scraper's price-comparison path works end-to-end the same way the original automatic-price-lookup feature's "never tested against a real site" caveat above still does.

### Framing text to copy-paste at the start of the next session

```
I'm picking the Trakka project back up (an ultra-lightweight Go backend + PWA for shopping/to-do lists, now with local+OIDC auth, house sharing plus granular Space/List sharing with a second user (including pinning a shared list to your own dashboard), automatic item-price lookup, an FR/EN i18n layer, a purchase-planning/budget view, recurring items, urgent items, price-drop deal notifications, a Light/Dark/Auto theme system, per-user custom categories/"Spaces" for lists, a versioned SQLite migration engine with pre-migration backups plus a service worker update banner, and a hardened, SHA-pinned GitHub Actions CI/CD pipeline — see CLAUDE.md at the repo root for all conventions and security rules). Read CLAUDE.md in full before starting, especially the "CI/CD & Sécurité" and "Project status and session handoff" sections.

State at handoff: the backend (complete REST v1 API + auth/RBAC + background price scraping + items.target_month + recurring items + items.is_urgent + price_alerts + custom_categories/lists.custom_category_id + space_shares/list_shares granular sharing + the versioned internal/db/migrations/ engine replacing the old schema.sql), the PWA frontend (app.js, list_view.js, planning.js, urgent.js, spaces.js, shares.js, notifications.js, i18n.js, theme.js/theme-init.js/tailwind-config.js, db.js, sw.js, templates/login.html), offline support, the Dockerfile/compose.yml, and all documentation (docs/*.md) are already written and working (build/vet/gofmt clean, `go test ./...` passing for the packages that have tests). Check `git status`/`git log` before any destructive action.

Open points to prioritize (see CLAUDE.md for details):
0. Exercise the item-quantity fix in a real browser — price × quantity in an item's row subtotal, the finance summary bar, the dashboard/Espaces card total badge, and the planning view's totals, plus the new [-]/[+] stepper's rapid-click/rollback behavior. Only `go build`/`go vet`/`gofmt`/`node --check` were run when this was built, no browser.
1. Exercise the granular sharing frontend in a real browser — the backend was verified extensively (Go tests + a live curl walkthrough of every access-control edge case), but `static/js/shares.js` (the share modal, the 👥 buttons, the "Partagé avec moi" tab) has not been clicked through yet — no browser automation tooling was available in the sandbox that built it.
3. Exercise the custom categories/"Spaces" frontend in a real browser — the backend and UI (schema, `internal/db`, `internal/handlers`, `static/js/spaces.js`, the "new list" modal's category picker) are both done, but no browser automation tooling was available in the sandbox that built the frontend, so none of it (empty state, category CRUD, the nested create-space-from-list-modal flow, mixed-type grouping) has actually been clicked through yet.
4. Verify the price alerts feature live: no product page with a real, triggerable price drop was available in the sandbox that built it, and no browser exercised the 🔔 bell/badge/drawer — see the dedicated bullet under "What's left" for a concrete verification recipe.
5. Test the OIDC flow against a real identity provider (Authelia/Keycloak/Google) — it was never exercised end-to-end, only built to spec.
6. Try the automatic price lookup against a real product page (Shopify/Amazon/Fnac/...) — only verified against synthetic HTML fixtures and a price-less example.com so far.
7. Verify the Docker image actually builds and runs (no Docker daemon has been available in any sandbox so far, including when the templates/ COPY was added and, most recently, when the runtime base was switched to distroless with a read-only root filesystem) — the `read_only: true` change is the part most likely to surface a real problem, since it's the first time anything has actually run the container rather than just reasoning about what it writes.
8. Consider a minimal test suite for the remaining security-sensitive session/OIDC code, plus a new one for internal/validate (URL and Month) and the target_month DB/handler paths.
9. Wire up "rename/delete house" and "leave house" buttons in the dashboard UI.
10. Translate templates/login.html and the remaining hard-coded-French dynamic JS strings (item counts, "Terminés (N)", error messages, empty states) to finish what the FR/EN i18n session started.
11. Exercise the urgent-items frontend in an actual browser (checkbox, badge, sort-to-top, the "Urgent" dashboard tab) — only the backend was verified when it was built.
12. Exercise the new offline UX pass in an actual browser: go offline (DevTools Network → Offline) after visiting the Budget/Urgent/Espaces tabs once online, confirm all three keep showing cached data instead of going blank, and confirm no error banner pops during ordinary offline navigation (only the header's "Hors-ligne" badge should show) — see the dedicated "Offline UX pass" bullet under "What's done" for the exact mechanism (`isNetworkError`, `cachedDashboardLists` reuse, the new `custom_categories` IndexedDB store).
13. Exercise the new service worker update banner in a real deployment-and-reload cycle (bump `sw.js`'s cache version, redeploy, reload an already-open tab, confirm the banner appears and its button reloads onto the new assets), and confirm the migration engine's `hasExistingSchema` adoption path against a real, full-sized pre-existing `/data/trakka.db` rather than just the synthetic minimal seed the Go test uses — see the "Versioned SQLite migration engine..." bullet under "What's done".
14. Push a commit or open a PR to actually exercise `.github/workflows/ci.yml` on GitHub's runners for the first time — every Go-side tool it calls was verified locally and the YAML's structure was validated separately, but the Docker build, both Trivy scans, the SARIF uploads to the Security tab, and the build-provenance attestation have never actually run. See the "CI/CD & Sécurité" section for the full pipeline design.
15. The core pinning flow (List share, and a House-visible Space both same- and cross-House) is now browser-verified end to end via Playwright, including the "no reload needed" dashboard refresh fix — see the "Pinning: dashboard didn't refresh..." bullet in "What's done". Still to click through: the `[⋮]` kebab/bottom-sheet pin/unpin variant on a narrow viewport (only desktop was exercised this session) for both a shared List card and a Space card, and pinning a whole *explicitly-shared* (`space_share`) Space causing every one of its Lists to appear on the dashboard at once (only the `house_member` pin path's dashboard-appearance behavior was scripted).

Don't recreate anything that already exists — inspect the existing code and docs first before proposing changes.
```

## Project conventions

Inventory of conventions established while building Trakka, to be followed for any future contribution.

### Backend (Go)

- Standard library only wherever possible: `net/http` (no third-party framework/router), `log/slog` for structured JSON logging, `database/sql` for data access, a hand-rolled OIDC/JWT client (`crypto/rsa`, `crypto/sha256`, `encoding/json`, `net/http`) rather than a third-party OIDC library. The only notable external dependencies are `modernc.org/sqlite`, `golang.org/x/crypto` (bcrypt only), and `golang.org/x/net/html` (used by `internal/scraper` to parse a fetched product page's HTML rather than regex-scraping it) — the two `golang.org/x/...` packages are quasi-stdlib, maintained by the Go team itself, so neither is really a step away from this convention.
- Strict package split (`cmd/server`, `internal/config`, `internal/models`, `internal/db`, `internal/auth`, `internal/handlers`, `internal/validate`) with a hard boundary: `internal/handlers` never imports `database/sql` and only knows about the methods exposed by `*db.DB` and the `db.ErrNotFound` sentinel. `internal/auth` holds non-HTTP auth logic (bcrypt, session tokens, the OIDC client) and is imported by `internal/handlers`, never the reverse.
- Routing via the `mux.HandleFunc("METHOD /path", ...)` syntax from the Go 1.22+ stdlib, never a third-party router (no Gin/chi/etc.).
- All SQL queries are parameterized (`?`), confined to `internal/db`, never built by concatenation/`fmt.Sprintf`.
- Schema versioned through a sequence of numbered migration files (`internal/db/migrations/NNNN_description.sql`), embedded via `//go:embed` and tracked through `PRAGMA user_version` (`internal/db/migrate.go`) — no third-party migration framework, consistent with the stdlib-first convention above; an already-shipped migration file is never edited in place, and a pending migration against an already-versioned database is preceded by an automatic `VACUUM INTO` backup.
- SQLite booleans stored as `0`/`1`, converted to Go `bool` in the `scan*` functions.
- Timestamps in UTC, ISO-8601 with milliseconds; IDs are positive `AUTOINCREMENT` integers.
- Config exclusively via environment variables, loaded once in `internal/config.Load()` — no config file, no CLI flags other than `-healthcheck`.
- Predictable errors handled explicitly (`if err != nil`) everywhere; the `Recover` middleware is a safety net for panics, not a normal error-handling mechanism.
- JSON responses go through centralized helpers (`decodeJSON`/`writeJSON`/`writeError`/`pathID` in `internal/handlers/json.go`) rather than per-handler duplicated code; unknown JSON fields in a request body are rejected (`400`), body capped at 1 MiB.
- `go build`, `go vet ./...`, `gofmt -l .`, and `golangci-lint run ./...` (config: `.golangci.yml`) must stay clean on every change — all four are enforced in CI (see "CI/CD & Sécurité" above).

### Frontend (PWA, `static/`)

- Vanilla ES6+ JavaScript (`async`/`await`), **no dependencies, no build step, no framework**. Several classic `<script>` tags (`app.js`, `list_view.js`, `db.js`) share the same global scope instead of using ES modules — identifiers (`state`, `els`, `apiRequest`, etc.) are therefore shared implicitly across files.
- Every network call goes through the `apiRequest()` helper (never a raw `fetch` elsewhere), which centralizes error handling via an on-page error banner and redirects to `/auth/login` on a `401` (the single choke point that auth-gates the whole SPA). `/auth/...` endpoints (login, register, logout, OIDC) are classic form-`POST`s with full-page redirects instead, served from `templates/login.html` — not part of the `apiRequest()`/JSON world.
- DOM built exclusively via `document.createElement`/`textContent`, **never** `innerHTML` with interpolated data — defense in depth against XSS.
- Any URL rendered as an `<a href>` is re-validated client-side (`http(s)` scheme only) before use, mirroring the server-side validation (`internal/validate.URL`).
- CSS: Tailwind CSS via the "Play" CDN (`https://cdn.tailwindcss.com`), no local Tailwind build; supplemented by a minimum of custom CSS (`static/css/tokens.css` for design tokens, `static/css/base.css`).
- `db.js` never references `window`/`document` (only `indexedDB`/`self`) since it's loaded both as a page script and via the service worker's `importScripts()`.
- The service worker (`sw.js`) is the **sole writer** of the IndexedDB mirror and the offline write queue; `app.js`/`list_view.js` only read (`TrakkaDB.getQueue()`) to display a badge.
- Service worker caches are version-suffixed (`trakka-shell-v1`, etc.) — bump the suffix on every `APP_SHELL` change.
- **UI language: French by default, with an FR/EN toggle** — `static/js/i18n.js` (loaded before every other page script) detects `navigator.language` or a saved `localStorage['trakka:lang']` choice, fetches `static/locales/{fr,en}.json`, and applies it to any element flagged `data-i18n`/`data-i18n-placeholder`/`data-i18n-aria-label`; a discreet dropdown in the header (`#lang-button`/`#lang-menu`) lets the user switch and persists the choice. Static markup goes through those `data-i18n*` attributes; dynamic strings built in JS (`app.js`, `list_view.js`, `planning.js`) call `window.TrakkaI18n.t('some.key', vars)` (or the local `t()` wrapper in `app.js`) instead of hard-coding text — not every dynamic string has been converted yet (see "What's left" in the handoff section below), so grep for a literal French string before assuming it's already wired up. Code, comments, and documentation stay in English regardless of UI language.

### Docker / deployment

- Multi-stage build: `golang:X.Y.Z-alpine` (builder, `CGO_ENABLED=0`, static + stripped binary) → `gcr.io/distroless/static-debian12` (runtime — no shell, no package manager). The Dockerfile's Go version, `go.mod`'s `go` directive, and the `modernc.org/sqlite` version must stay in sync (see the Architecture section above); the distroless runtime tag isn't part of that lockstep and should be pinned to a digest for production.
- Prefer a shell-less distroless/Chainguard-style runtime base over a general-purpose one (Alpine, Debian) whenever the compiled artifact is a static binary with no runtime dependency on a package manager or shell — smaller attack surface, no incentive to `apk add`/`apt install` something later. Alpine stays fine for a *builder* stage, which needs a shell/package manager to compile.
- Non-root user with a **fixed numeric UID:GID** (`10001:10001`, via `adduser -u 10001` then `USER 10001:10001`), never a symbolic `USER` — needed for predictable behavior under Podman rootless.
- `compose.yml` must stay valid for both `docker compose` **and** `podman-compose` at once — avoid any Compose field not supported by both.
- Named volumes only (never bind mounts), to avoid UID permission issues under rootless.
- Optional services (e.g. `radicale`) gated behind a Compose profile (`calendar`), never started by default.
- `security_opt: no-new-privileges:true`, no privileged capabilities, no host networking.
- Trakka's own service runs `read_only: true` with `cap_drop: [ALL]`, since a static Go binary talking to SQLite over a local named volume needs to write nowhere but `/data` (and, defensively, `/tmp`) and needs no Linux capability at all. This is specific to `trakka` itself, not a blanket rule for every service in `compose.yml` — a third-party image like `radicale` that wasn't built with a read-only root filesystem in mind is left out of it.

### Documentation

- One file per topic under `docs/` (`API.md`, `DATABASE.md`, `DEPLOYMENT.md`, `DEVELOPMENT.md`, `PWA.md`), heavily cross-linked to each other and to `CLAUDE.md` via relative Markdown links.
- `CLAUDE.md` is the reference for cross-cutting rules and invariants (version pinning, security rules) — topic docs link back to it rather than duplicating the information.
- Docs explain the **why**, not just the what (e.g. why `ALTER TABLE` must be guarded manually, why a given bug was found by testing rather than by review).
- Code, comments, and technical documentation are in English; only end-user-visible strings (the UI) are in French.

### Git

- Only one commit exists so far ("Initial commit") — no commit message convention has been established in this project yet; to be defined at the first real commit of application code.
